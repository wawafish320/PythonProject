# C++ / Python Inference Alignment

Canonical document: `docs/CPP_PYTHON_INFERENCE_ALIGNMENT.md`

This root-level file is intentionally kept as a short pointer so the inference contract has a single maintained source.
