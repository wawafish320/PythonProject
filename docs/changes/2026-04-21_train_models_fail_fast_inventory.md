# [2026-04-21] `train/models.py` fail-fast inventory

Date: 2026-04-21
Scope: Phase A first step only — inventory and documentation backfill for broad `except Exception` handlers in `train/models.py`.
Non-goal: no behavior change, no helper extraction, no direct-pose contract typing, no file split.

---

## Execution Summary

- **本轮范围**：只读取 `AGENTS.md`、`docs/removal_policy.md`、`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`、`train/MODULE_BOUNDARIES.md`，并对 `train/models.py` 当前 broad exception handler 做 inventory。
- **统计摘要**：初始 inventory 基线为 `72` 个 broad `Exception` handler；其中 `71` 个是精确文本 `except Exception:`，另有 `1` 个是 `except Exception as exc:`（原 `train/models.py:3685`，已在 Batch 5 收窄）。截至 `2026-04-23` C.loss.3 + Category C patch 后，源码当前 broad `Exception` handler 为 `0`。
- **分类结果**：初始 inventory 类别 A `51` 条，类别 B `19` 条，类别 C `2` 条；截至 `2026-04-23`，类别 A / B / C 已全部清零。
- **热点一致性**：实际热点与 roadmap 一致，集中在 `EventMotionModel.forward(...)`（32 条）、direct-pose model/runtime、以及 `MotionJointLoss`（12 条）；roadmap 原 `71` 处是精确 `except Exception:` 计数，若按 broad handler 口径应更新为 `72`。
- **本轮改动**：初始阶段新增本 inventory 文档；截至当前已同步回填 A01–A51 的 fail-fast 落地、测试和 broad handler 计数变化，并在 `2026-04-22` 补齐 A4 `_direct_pose_split_state()` strict failure / generic `forward` shape regression 覆盖、完成 Phase A Category B constructor normalization B01–B11，在 `2026-04-23` 完成 Category B 其余 broad cluster B12–B19，并完成 Phase B.B1 pure helper module-level 化、Phase B.B3 skeleton / weight-cache in-file cluster、Phase B.B4 applicators in-file cluster，以及 Phase B.B5 rot6d in-file cluster。

### High-risk Hotspots

1. Phase A Category A 已清零；A30 已保留 contextual wrap 但从 broad `Exception as exc` 收窄为 typed contextual re-raise。
2. 类别 B 的 numeric/config/runtime/build/aux fallback 已完成 B01–B19；Category C compiler/export probes 已在 `2026-04-23` 收窄完成。
3. build-time layout / deterministic init / routing metadata cluster（A31–A40）已在 2026-04-21 Batch 3 落地。
4. MotionJointLoss config / skeleton / payload scalar resolver cluster（A41–A51）已在 2026-04-21 Batch 4 落地。
5. broad handler 已归零；后续如再引入新的 broad `Exception` handler，应视为新 debt 单独登记。

### Recommended Next Order

1. broad handler inventory 已完成收口；后续重点从 fail-fast inventory 转到 Phase D / Phase E 的结构化收敛。
2. A4 覆盖缺口、constructor B01–B11、B12–B18 helper/runtime 与 B19 attention regularization 回归已在 `2026-04-23` 补齐。
3. 当前可直接进入 Phase D / Phase E；无需再为 broad handler 留专项 batch。
4. 后续仍需保持 removal-policy grep：不得新增 nested fallback、warning-only、silent ckpt rename、compat/legacy 注释债。

### Implementation Update — 2026-04-21

- **本轮目标**：执行类别 A 前两个最高优先级 direct-pose `forward` 大吞错点，不做 helper 提取、不做文件拆分。
- **实际完成项**：A01 / A02 已从 silent `pass` 改为窄异常捕获 + fail-fast `RuntimeError` 上下文；新增 `tests/train/test_train_models_failfast.py` 覆盖 non-side 与 side-routed leg residual head failure。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `72` 降到 `70`；精确 `except Exception:` 从 `71` 降到 `69`；`except Exception as exc` 仍为 `1`（outer direct-pose wrapper）。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py`、`python3 -m unittest tests.train.test_train_models_failfast`、`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d` 均通过。
- **下一步**：继续处理 A03–A07（gate / scale / sign gate 的 direct-pose leg residual 静默 fallback），再处理 A12 `train/models.py:2406` 的 `_expand_state_sequence(...)`。

### Implementation Update — 2026-04-21 A03–A07

- **本轮目标**：只处理 A03–A07 的 direct-pose leg gate / scale / sign-gate inner fallback，不做 helper 提取、不做文件拆分。
- **实际完成项**：A03 / A04 / A05 / A06 / A07 已从 silent fallback 改为窄异常捕获 + fail-fast `RuntimeError` 上下文；`tests/train/test_train_models_failfast.py` 新增 5 个 regression tests 覆盖 side-routed learned gate、side-routed scale gate、side sign gate、non-side learned gate、non-side scale gate failure。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `70` 降到 `65`；精确 `except Exception:` 从 `69` 降到 `64`；`except Exception as exc` 仍为 `1`（outer direct-pose wrapper）。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py`、`python3 -m unittest tests.train.test_train_models_failfast`、`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d` 均通过。
- **下一步**：处理 A12 `train/models.py:2406` 的 `_expand_state_sequence(...)`，然后继续 A08–A11 direct-pose feature/phase/time input contract。

### Implementation Update — 2026-04-21 A12

- **本轮目标**：只处理 A12 `train/models.py:2406` 的 `_expand_state_sequence(...)`，把 silent `None` 收窄为 fail-fast，不做 helper 提取、不做文件拆分。
- **实际完成项**：A12 已落地；`_expand_state_sequence(...)` 现要求 `phase_z` / `phase_event_age` 满足显式 broadcast contract，并在失败时抛出带字段名、期望 broadcast 语义、实际 `shape` / `ndim`、`B` / `Tq` / `feat_dim` 上下文的 `RuntimeError`；调用点最小调整为显式传入 `field_name`。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_phase_z_sequence_contract_failure_raises` 与 `test_phase_event_age_sequence_contract_failure_raises`，当前 fail-fast regression 总数为 `9`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `65` 降到 `64`；精确 `except Exception:` 从 `64` 降到 `63`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...`、`sed -n '2380,2475p' train/models.py | rg ...` 均通过或复核一致。
- **下一优先级热点**：A08–A11，优先从 `train/models.py:3294` 的 per-side phase view failure 开始，再收窄 `3137` / `3122` / `3383` 的 direct-pose feature / time / side-embed contract。

### Implementation Update — 2026-04-21 A08

- **本轮目标**：只处理 A08 `train/models.py:3294` 的 side-routed per-side phase view failure，去掉 malformed `phase_z_in_direct` 被零填充的 silent degradation，不做 helper 提取、不做文件拆分。
- **实际完成项**：A08 已落地；side-routed direct-pose leg branch 现在要求 `phase_z_in_direct` 在 per-side view 前严格为 `(B, Tq, 2*contact_channels)`，失败时抛出包含 `phase_z_in_direct`、期望 `(B, Tq, contact_channels, 2)` view 语义、实际 `shape` / `ndim`、`ch_r` / `ch_l` / `contact_dim` 的 `RuntimeError`。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_side_routed_phase_z_view_contract_failure_raises`，当前 fail-fast regression 总数为 `10`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `64` 降到 `63`；精确 `except Exception:` 从 `63` 降到 `62`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：A09–A11，优先处理 `train/models.py:3383` 的 side embedding failure，然后处理 `3137` direct-pose time PE concat failure 与 `3122` feature-source fallback。

### Implementation Update — 2026-04-21 A09

- **本轮目标**：只处理 A09 `train/models.py:3383` 的 side embedding failure，把 enabled side embedding 被静默 drop 的 fallback 改成 fail-fast，不做 helper 提取、不做文件拆分。
- **实际完成项**：A09 已落地；side-routed direct-pose leg branch 现在要求 `direct_pose_leg_side_embed` 成功产出可 broadcast 到 `(B, Tq, D)` 的 right/left side embedding，失败时抛出包含期望 broadcast 语义、`embed_weight_shape`、实际 `emb_r.shape` / `emb_l.shape` 的 `RuntimeError`。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_side_routed_side_embedding_failure_raises`，当前 fail-fast regression 总数为 `11`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `63` 降到 `62`；精确 `except Exception:` 从 `62` 降到 `61`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：A10–A11，优先处理 `train/models.py:3137` 的 direct-pose time PE concat failure，然后处理 `3122` feature-source fallback。

### Implementation Update — 2026-04-21 A10

- **本轮目标**：只处理 A10 `train/models.py:3137` 的 direct-pose time PE concat failure，把 ignore fallback 改成 fail-fast，不做 helper 提取、不做文件拆分。
- **实际完成项**：A10 已落地；direct-pose 分支现在要求 `direct_feat` 与 `time_pe_direct` 在 concat 前满足 `(B, Tq, F)` / `(B, Tq, time_pe_dim)` contract，concat 或 `to(...)` 失败时抛出包含 `direct_feat.shape`、`time_pe_direct.shape`、`time_pe_dim` 的 `RuntimeError`。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_direct_pose_time_pe_concat_failure_raises`，当前 fail-fast regression 总数为 `12`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `62` 降到 `61`；精确 `except Exception:` 从 `61` 降到 `60`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：A11，优先处理 `train/models.py:3122` 的 `direct_pose_feat_source` fallback；随后再回到 contact-plan / time feature 条目。

### Implementation Update — 2026-04-21 A11 + A14–A16 batch

- **本轮目标**：按相邻 `forward` cluster 批处理 A11 + A14 + A15 + A16：收紧 `direct_pose_feat_source`、`time_index` 规范化、contact/direct time PE 构造，不做 helper 提取、不做文件拆分。
- **实际完成项**：A11 / A14 / A15 / A16 已一起落地；`direct_pose_feat_source` 不再在 init 或 forward 中 silent fallback 到 `cond`，`time_index` 现在要求显式满足 broadcast 到 `(B, Tq)` 的 contract，contact-plan 与 direct-pose time PE 构造失败时都会抛出带 `pe_dim` / `t_grid.shape` / `base` 上下文的 `RuntimeError`。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_direct_pose_feat_source_contract_failure_raises`、`test_time_index_contract_failure_raises`、`test_contact_plan_time_pe_construction_failure_raises`、`test_direct_pose_time_pe_construction_failure_raises`，当前 fail-fast regression 总数为 `16`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `61` 降到 `57`；精确 `except Exception:` 从 `60` 降到 `56`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：contact-plan / phase / side-cue cluster，优先处理 `2513`、`2721`、`2730`、`2810`、`2819`、`2847`、`2852`、`2856`，随后处理 `2767` / `2830`。

### Implementation Update — 2026-04-21 A13 + A19–A27 batch

- **本轮目标**：按相邻 `forward` cluster 批处理 A13、A19–A27：收紧 contact-plan observed init、event-clock on/off phase append、side-cue append、time bias、以及 phase/cue/logits stack，不做 helper 提取、不做文件拆分。
- **实际完成项**：A13、A19–A27 已一起落地；`contact_plan_init_mode in {'obs', 'learnable+obs'}` 不再在 init head 失败时 silent fallback，event-clock on/off 的 phase / side-cue append 不再 `pass`，enabled time bias 不再静默丢掉，`phase_in_direct_seq` / `leg_side_cue_seq` / `plan_logits` 的 stack 失败现在都会带元素 shape 和 step 上下文 fail-fast。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_contact_plan_observed_init_failure_raises`、`test_event_clock_phase_append_failure_raises`、`test_event_clock_side_cue_append_failure_raises`、`test_non_event_clock_phase_append_failure_raises`、`test_non_event_clock_side_cue_append_failure_raises`、`test_event_clock_time_bias_failure_raises`、`test_non_event_clock_time_bias_failure_raises`、`test_phase_sequence_stack_failure_raises`、`test_side_cue_sequence_stack_failure_raises`、`test_contacts_plan_logits_stack_failure_raises`，当前 fail-fast regression 总数为 `26`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `57` 降到 `47`；精确 `except Exception:` 从 `56` 降到 `46`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：`forward` auxiliary / late-forward cluster，优先处理 `2664`、`2708`、`3740`、`3755`；之后再切 build-time cluster。

### Implementation Update — 2026-04-21 A17/A18/A28/A29 batch

- **本轮目标**：按 `forward` auxiliary / late-forward cluster 批处理 A17、A18、A28、A29：收紧 adaptive-history、frozen period feature、lambda-fusion rollout-step 与 lambda-fusion head，不做 helper 提取、不做文件拆分。
- **实际完成项**：A17 / A18 / A28 / A29 已一起落地；event-clock adaptive-history 与 frozen period feature 失败不再 silent degradation，lambda-fusion rollout-step 输入不再退化为零 step feature，lambda-fusion head 失败不再吞掉 `lambda_fusion` 输出。
- **新增测试**：`tests/train/test_train_models_failfast.py` 新增 `test_adaptive_history_failure_raises`、`test_frozen_period_feature_failure_raises`、`test_lambda_fusion_rollout_step_contract_failure_raises`、`test_lambda_fusion_forward_failure_raises`，当前 fail-fast regression 总数为 `30`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `47` 降到 `43`；精确 `except Exception:` 从 `46` 降到 `42`；`except Exception as exc` 仍为 `1`。
- **验证结果**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`、`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`、`python3 - <<'PY' ... ast.parse(train/models.py) ...` 均通过或复核一致。
- **下一优先级热点**：build-time layout / deterministic init / routing metadata cluster：`1533`、`1556`、`1568`、`1588`、`1190`、`1206`、`980`、`1019`、`1144`、`1151`。

### Implementation Update — 2026-04-21 Batch 3 build-time layout / init / routing metadata cluster

- **本轮目标**：按 build-time cluster 一起处理 A31–A40：收紧 lambda/so3 rot6d layout 推断、contact-plan deterministic init、direct-pose routing metadata register / side-embed init，不做 helper 提取、不做文件拆分。
- **实际完成项**：A31 / A32 不再在 rot6d layout 推断失败时把 joint count 静默置 `0`；A33 / A34 / A35 / A36 不再吞掉 deterministic zero/logit init 失败；A37 / A38 / A39 不再吞掉 routing metadata buffer register/update 失败；A40 不再吞掉 side embedding zero-init 失败，全部改为带字段名、期望 shape / module path、实际 shape / type 的 fail-fast。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests/train/test_train_models_failfast.py` 新增 `test_lambda_fusion_rot6d_layout_failure_raises_at_build_time`、`test_so3_corrector_rot6d_layout_failure_raises_at_build_time`、`test_lambda_fusion_deterministic_init_failure_raises_at_build_time`、`test_so3_corrector_deterministic_init_failure_raises_at_build_time`、`test_contact_plan_init_head_deterministic_init_failure_raises_at_build_time`、`test_contact_plan_time_head_deterministic_init_failure_raises_at_build_time`、`test_direct_pose_leg_joint_index_buffer_registration_failure_raises`、`test_direct_pose_split_leg_index_buffer_registration_failure_raises`、`test_direct_pose_side_position_buffer_registration_failure_raises`、`test_direct_pose_side_embedding_deterministic_init_failure_raises_at_build_time`；当前 fail-fast regression 总数为 `40`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `43` 降到 `33`；精确 `except Exception:` 从 `42` 降到 `32`；`except Exception as exc` 仍为 `1`；类别 A 原始条目剩余 `12`（A30 + A41–A51）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '970,1265p' train/models.py; sed -n '1520,1685p' train/models.py; sed -n '1,220p' tests/train/test_train_models_failfast.py; sed -n '760,980p' tests/train/test_train_models_failfast.py) | rg ...`。
- **验证结果**：全部通过；`py_compile` 通过，`unittest` 共 `46` 个用例通过，AST 计数与源码一致，Batch 3 touched ranges 的 removal-policy 反模式 grep 未命中新增 nested fallback / warning-only / silent rename / compat 注释。
- **阻塞项 / 风险**：Batch 4 将首次触碰 `MotionJointLoss` config / skeleton / payload scalar resolver；其中 arm/else weight、ratio min/max、EMA beta、epsilon 的有效范围需要保持“valid input 语义不变、invalid input fail-fast”。
- **下一优先级热点**：进入 Batch 4，处理 A41–A51 的 MotionJointLoss config / skeleton / payload cluster；Batch 4 完成后再判断 A30 outer direct-pose wrapper 是否保留 contextual wrap。

### Implementation Update — 2026-04-21 Batch 4 MotionJointLoss config / skeleton / payload cluster

- **本轮目标**：按 MotionJointLoss cluster 一起处理 A41–A51：收紧 direct-pose arm/else weight、group-norm beta / ratio / eps、skeleton offsets、payload override scalar resolver，不改 loss 数学语义，不加 default fallback / warning-only。
- **实际完成项**：A41 / A42 / A43 / A44 / A45 / A46 现对 invalid scalar / range 直接 fail-fast；A47 / A48 现对 invalid skeleton offsets type / shape / finite 性质直接 fail-fast；A49 / A50 现对 invalid payload `arm_weight` / `else_weight` override 直接 fail-fast；A51 现把 runtime scalar resolver 改成 typed scalar 校验，不再吞掉 bad override。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests/train/test_train_models_failfast.py` 新增 `test_motion_joint_loss_arm_weight_invalid_raises`、`test_motion_joint_loss_else_weight_invalid_raises`、`test_motion_joint_loss_group_norm_beta_invalid_raises`、`test_motion_joint_loss_group_norm_ratio_min_invalid_raises`、`test_motion_joint_loss_group_norm_ratio_max_invalid_raises`、`test_motion_joint_loss_group_norm_eps_invalid_raises`、`test_motion_joint_loss_ctor_skeleton_offsets_invalid_raises`、`test_motion_joint_loss_set_skeleton_offsets_invalid_raises`、`test_motion_joint_loss_payload_arm_weight_override_invalid_raises`、`test_motion_joint_loss_payload_else_weight_override_invalid_raises`、`test_motion_joint_loss_group_norm_runtime_scalar_invalid_raises`；当前 fail-fast regression 总数为 `51`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `33` 降到 `22`；精确 `except Exception:` 从 `32` 降到 `21`；`except Exception as exc` 仍为 `1`；类别 A 原始条目剩余 `1`（A30）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '4260,4320p' train/models.py; sed -n '4356,4528p' train/models.py; sed -n '5390,5515p' train/models.py; sed -n '1,220p' tests/train/test_train_models_failfast.py; sed -n '980,1140p' tests/train/test_train_models_failfast.py) | rg ...`。
- **验证结果**：全部通过；`py_compile` 通过，`unittest` 共 `57` 个用例通过，AST 计数与源码一致，Batch 4 touched ranges 经局部人工复核后未新增 nested fallback / warning-only / silent rename / compat 注释（唯一 `.get(...get...)` grep 命中为既有 `layout.get('slices') if isinstance(layout.get('slices'), dict)`，非 nested fallback）。
- **阻塞项 / 风险**：仅剩 A30 outer direct-pose wrapper；如改成 typed contextual wrap，需要同步确认现有 `direct_pose forward failed` 上下文仍保留且 broad handler 计数继续下降。
- **下一优先级热点**：进入 Batch 5，处理 A30 并重新扫描 `train/models.py` broad handler，确认 Phase A 是否已无 Category A 剩余。

### Implementation Update — 2026-04-21 Batch 5 Phase A sweep / A30

- **本轮目标**：重新扫描 `train/models.py` broad handler，对照 inventory 处理剩余 Phase A Category A；重点判断 A30 outer direct-pose wrapper 是保留 contextual wrap 还是让 typed errors 直出。
- **实际完成项**：A30 已处理；保留 `direct_pose forward failed` contextual wrap 以保留现有 direct-pose fail-fast 上下文，但把 `except Exception as exc` 收窄为 `(AttributeError, IndexError, KeyError, RuntimeError, TypeError, ValueError)` typed contextual re-raise。重新扫描确认剩余 broad handler 全部对应 Category B / C。
- **修改文件列表**：`train/models.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：未新增测试；现有 direct-pose fail-fast tests 继续覆盖 `direct_pose forward failed` contextual wrap，当前 fail-fast regression 总数仍为 `51`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `22` 降到 `21`；精确 `except Exception:` 为 `21`；`except Exception as exc` 从 `1` 降到 `0`；Phase A Category A 原始条目剩余 `0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`sed -n '4166,4180p' train/models.py | rg ...`。
- **验证结果**：全部通过；`py_compile` 通过，`unittest` 共 `57` 个用例通过，AST 计数得到 broad=`21` / exact=`21` / as_exc=`0`，A30 touched hunk 的 removal-policy 反模式 grep 未命中。
- **阻塞项 / 风险**：Phase A Category A 已无剩余；剩余 broad handler 为 Category B `19` + Category C `2`，后续处理 Category B 时要避免把明确 fallback 扩大成 silent fallback。
- **下一优先级热点**：建议下一轮按 Category B cluster 处理 constructor/runtime numeric fallback，或先补 `_direct_pose_split_state()` strict failure / generic forward shape 的 A4 coverage 缺口后再进入 Phase B。

### Implementation Update — 2026-04-22 Phase A A4 coverage completion

- **本轮目标**：补齐 A4 回归缺口，只覆盖 `_direct_pose_split_state()` strict failure 与 `EventMotionModel.forward(...)` generic input shape contract，不改算法语义、不进入 Phase B/C/D。
- **实际完成项**：`tests/train/test_train_models_failfast.py` 新增 4 个 regression tests，覆盖缺失 leg index tensor、split coverage mismatch、generic `state` shape mismatch、generic `cond` shape mismatch；现有 `train/models.py` A4 fail-fast 补丁已由回归锁定。
- **修改文件列表**：`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`test_direct_pose_split_state_missing_leg_index_tensor_raises`、`test_direct_pose_split_state_disjoint_coverage_mismatch_raises`、`test_event_motion_model_forward_state_shape_contract_failure_raises`、`test_event_motion_model_forward_cond_shape_contract_failure_raises`；`tests.train.test_train_models_failfast` 当前 fail-fast regression 总数增至 `55`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `21 -> 21`；精确 `except Exception:` 维持 `21 -> 21`；`except Exception as exc` 维持 `0 -> 0`；Phase A Category A 原始条目仍为 `0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '1684,1765p' train/models.py; sed -n '2470,2575p' train/models.py; sed -n '1035,1115p' tests/train/test_train_models_failfast.py) | rg ...`。
- **验证结果**：通过；`py_compile` 通过，`unittest` 共 `61` 个用例通过，AST 计数得到 broad=`21` / exact=`21` / as_exc=`0`，A4 touched ranges 的 removal-policy 反模式 grep 零命中（`rg` exit code `1`）。
- **阻塞项 / 风险**：Phase A 范围内已无 Category A 剩余；剩余 broad handler 为 Category B `19` + Category C `2`，若继续清理需保持 explicit fallback，不可重新引入 silent fallback / warning-only debt。
- **下一优先级热点**：优先按 Category B cluster 处理 constructor/runtime numeric fallback，或在保持语义冻结前提下启动 Phase B in-file helper / cluster 化。

### Implementation Update — 2026-04-22 Phase A Category B constructor normalization B01–B11

- **本轮目标**：只处理 `EventMotionModel.__init__` 的 constructor normalization / config fallback cluster，清理 inventory B01–B11，不进入 runtime / `MotionJointLoss` / helper 提取 / 文件拆分。
- **实际完成项**：`train/models.py` 的 B01–B11 已全部落地：去掉 `direct_pose_phase_z_mode`、`direct_pose_leg_mode`、`direct_pose_leg_max_deg`、`direct_pose_leg_gate_mode`、`direct_pose_leg_gate_power`、`direct_pose_leg_scale_log_clip`、`direct_pose_leg_scale_clamp_k`、`direct_pose_leg_contact_order`、`direct_pose_leg_side_embed_dim`、`direct_pose_leg_side_cue`、`direct_pose_leg_side_cue_tau` 上的 broad `except Exception`；保留 omission / `None` 默认语义；对 unknown string mode 保留 typed fallback 到显式默认值；对 exotic non-string object 与 numeric parse/range failure 改为 constructor-time `TypeError` / `ValueError`；对 `scale_clamp_k <= 1` 与 negative `side_embed_dim` 继续保留显式禁用/钳制语义。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests/train.test_train_models_failfast` 新增 `15` 个 constructor regression tests：`test_direct_pose_ctor_unknown_string_modes_use_explicit_defaults`、`test_direct_pose_ctor_none_defaults_preserved_for_constructor_cluster`、`test_direct_pose_phase_z_mode_exotic_object_raises`、`test_direct_pose_leg_mode_exotic_object_raises`、`test_direct_pose_leg_max_deg_invalid_range_raises`、`test_direct_pose_leg_gate_mode_exotic_object_raises`、`test_direct_pose_leg_gate_power_invalid_range_raises`、`test_direct_pose_leg_scale_log_clip_invalid_range_raises`、`test_direct_pose_leg_scale_clamp_k_invalid_type_raises`、`test_direct_pose_leg_scale_clamp_k_values_le_one_disable_explicitly`、`test_direct_pose_leg_contact_order_exotic_object_raises`、`test_direct_pose_leg_side_embed_dim_invalid_type_raises`、`test_direct_pose_leg_side_embed_dim_negative_clamps_to_zero`、`test_direct_pose_leg_side_cue_exotic_object_raises`、`test_direct_pose_leg_side_cue_tau_invalid_range_raises`；`tests.train.test_train_models_failfast` 总数增至 `70`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `21` 降到 `10`；精确 `except Exception:` 从 `21` 降到 `10`；`except Exception as exc` 维持 `0`；剩余 broad handler 为 Category B `8`（B12–B19）+ Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '650,835p' train/models.py; sed -n '1,260p' tests/train/test_train_models_failfast.py; tail -n 120 tests/train/test_train_models_failfast.py) | rg ...`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `76` 个用例通过，AST 计数得到 broad=`10` / exact=`10` / as_exc=`0`，constructor touched hunks 的 removal-policy 反模式 grep 零命中（`rg` exit code `1`）。
- **阻塞项 / 风险**：本轮刻意保留 unknown string mode 的 typed fallback 与 `scale_clamp_k <= 1` / negative `side_embed_dim` 的显式 disable/clamp 语义，避免把 constructor normalization 直接扩成语义性 fail-fast；若下一轮希望继续收紧 explicit bad value，需要与 posttrain / ckpt contract 一起评估。
- **下一优先级热点**：继续处理 Category B 剩余 broad handler，优先 B12 `_init_bone_residual_adapters(...)`、B13 routing metadata name lookup、B14/B15 eval runtime scalar fallback、B16–B18 debug/ablation helper，再单独评估 B19。

### Implementation Update — 2026-04-23 Phase A Category B B12–B18 runtime/build helper cluster

- **本轮目标**：只处理 Category B 的 B12–B18，覆盖 `_init_bone_residual_adapters(...)`、split routing metadata name lookup、eval runtime scalar fallback、contact-plan debug stack helper、direct-pose cross-leg ablation helper；不进入 B19 / Category C / Phase B。
- **实际完成项**：`train/models.py` 中 B12–B18 已全部收窄：`_init_bone_residual_adapters(...)` 外层 wrapper 仅对 metadata 相关 `(KeyError, IndexError, TypeError, ValueError)` 保留 disable fallback，unexpected runtime/module error 直接上抛；split metadata name lookup 仅对 `(TypeError, IndexError)` 保留 empty-name fallback；runtime `contact_plan_inject_scale` / `contact_plan_time_bias_scale` 仅对 `None` 与 `TypeError` / `ValueError` fallback 到 `1.0`；contact-plan debug `_stack(...)` 仅对 `torch.stack` 的 `RuntimeError` 返回 `None`；cross-leg ablation 的 `contact_dim`/`joint_names` fallback 收窄为 `(TypeError, ValueError)`，runtime error 不再被 broad swallow。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `12` 个 regression tests：`test_bone_residual_adapter_metadata_failure_disables_adapters`、`test_bone_residual_adapter_runtime_failure_raises`、`test_direct_pose_split_leg_name_type_error_falls_back_to_empty_names`、`test_direct_pose_split_leg_name_runtime_error_not_swallowed`、`test_eval_runtime_control_scalar_parse_failure_falls_back_to_default`、`test_eval_runtime_control_scalar_runtime_error_not_swallowed`、`test_contact_plan_debug_stack_shape_mismatch_falls_back_to_none`、`test_contact_plan_debug_stack_type_error_not_swallowed`、`test_direct_pose_leg_cross_leg_ablation_contact_dim_parse_failure_returns_none`、`test_direct_pose_leg_cross_leg_ablation_contact_dim_runtime_error_not_swallowed`、`test_direct_pose_leg_cross_leg_ablation_joint_names_parse_failure_returns_none`、`test_direct_pose_leg_cross_leg_ablation_joint_names_runtime_error_not_swallowed`；`tests.train.test_train_models_failfast` 总数增至 `82`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `10` 降到 `3`；精确 `except Exception:` 从 `10` 降到 `3`；`except Exception as exc` 维持 `0`；剩余 broad handler 为 Category B `1`（B19）+ Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d && python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '930,1035p' train/models.py; sed -n '1098,1112p' train/models.py; sed -n '2180,2265p' train/models.py; sed -n '2470,2490p' train/models.py; sed -n '1,260p' tests/train/test_train_models_failfast.py; sed -n '470,640p' tests/train/test_train_models_failfast.py) | rg ...`。
- **验证结果**：通过；联合 `unittest` 共 `88` 个用例通过，AST 计数得到 broad=`3` / exact=`3` / as_exc=`0`，B12–B18 touched hunks 的 removal-policy 反模式 grep 零命中（`rg` exit code `1`）。
- **阻塞项 / 风险**：B12 保留的是“metadata mismatch disables adapters”的显式设计，而不是 silent compat；B16 仍允许 debug-only shape mismatch 返回 `None`；B17/B18 仍允许 ablation helper 在 parse failure 时 skip analysis。上述 fallback 均已收窄到预期异常类型，但仍属设计内 optional/debug 行为。
- **下一优先级热点**：优先单独处理 B19 `MotionJointLoss.compute_attention_regularization`，完成后 `train/models.py` broad handler 将只剩 Category C compiler/export probes。

### Implementation Update — 2026-04-23 Phase A Category B B19 attention regularization

- **本轮目标**：只处理 B19 `MotionJointLoss.compute_attention_regularization` 的 geomask fallback，不进入 Category C / Phase B，不改 attention 正则主算法。
- **实际完成项**：`train/models.py` 中 B19 已收窄：`geomask` tensor 明确只接受 rank `2` / `3` / `4`；直接 broadcast 失败时只捕获 `RuntimeError`，rank-2 仅允许 `view(1,T,T)` fallback，rank-4 仅允许 `mean(0)` fallback，fallback 仍不能 broadcast 时显式 `RuntimeError`；non-tensor `geomask` 保留 explicit distance-prior path，不再依赖 broad `except Exception`。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `5` 个 attention regularization regression tests：`test_attention_regularization_non_tensor_geomask_uses_distance_prior`、`test_attention_regularization_geomask_invalid_rank_raises`、`test_attention_regularization_geomask_rank2_bad_shape_raises`、`test_attention_regularization_geomask_rank3_bad_broadcast_raises`、`test_attention_regularization_geomask_rank4_bad_fallback_raises`；`tests.train.test_train_models_failfast` 总数增至 `87`。
- **当前统计**：`train/models.py` broad `Exception` handler 从 `3` 降到 `2`；精确 `except Exception:` 从 `3` 降到 `2`；`except Exception as exc` 维持 `0`；剩余 broad handler 均为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d && python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '5245,5305p' train/models.py; sed -n '1545,1645p' tests/train/test_train_models_failfast.py; sed -n '171,210p' docs/changes/2026-04-21_train_models_fail_fast_inventory.md; sed -n '559,610p' docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md) | rg ...`。
- **验证结果**：通过；联合 `unittest` 共 `93` 个用例通过，AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，B19 touched hunks 的 removal-policy 反模式 grep 零命中（`rg` exit code `1`）。
- **阻塞项 / 风险**：B19 保留 non-tensor `geomask` 的 distance-prior explicit fallback；rank-4 `mean(0)` fallback 仍是原有 auxiliary regularization 的兼容形态，但现在 fallback 失败会显式报错，不再切到任意 alternate path。
- **下一优先级热点**：Phase A Category B 已清零；下一步建议要么保留 C01/C02 到 compiler/export 专项，要么进入 Phase B 的 single-file helper / cluster 化。

### Implementation Update — 2026-04-23 Phase B B1 pure helper free-function batch

- **本轮目标**：进入 Phase B，但只做 `train/models.py` 单文件内的 helper / cluster 化准备；本 batch 仅处理 `MotionJointLoss` 的 pure helpers：`_masked_group_mean`、`_masked_group_weighted_mean`、`_stats_float`、`_stats_float_or`、`_ensure_temporal_axis`、`_setdefault_stats`；不处理 Category C，不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑，不改 loss 语义。
- **实际完成项**：上述 6 个 pure helpers 已从 `MotionJointLoss` 类体实现提到 `train/models.py` 模块级 private free functions；`MotionJointLoss` 内部调用点已改为直接使用模块级 helper；为保持仓内现有私有调用面稳定，类上保留同名 `staticmethod` alias，因此 `train/posttrain.py` 等现有调用点无需改动；算法语义、默认超参、stats key、checkpoint contract 与数值结果均保持不变。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `4` 个 helper regression tests：`test_motion_joint_loss_module_group_helpers_preserve_weighted_means`、`test_motion_joint_loss_module_stats_helpers_preserve_scalar_contract`、`test_motion_joint_loss_module_temporal_and_stats_default_helpers`、`test_motion_joint_loss_helper_aliases_remain_compatible`；`tests.train.test_train_models_failfast` 总数增至 `91`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '4496,4560p' train/models.py; sed -n '5574,5590p' train/models.py; sed -n '5678,5688p' train/models.py; sed -n '5838,5852p' train/models.py; sed -n '6136,6172p' train/models.py; sed -n '6266,6272p' train/models.py; sed -n '6384,6430p' train/models.py; sed -n '1,20p' tests/train/test_train_models_failfast.py; sed -n '310,355p' tests/train/test_train_models_failfast.py) | if rg ...`.
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `97` 个用例通过（`tests.train.test_train_models_failfast=91`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮只完成 Phase B.B1 的 pure helper 边界显式化；`MotionJointLoss` 仍未完成 B3 skeleton / weight-cache cluster、B4 applicators cluster、B5 rot6d cluster；Category C 的 C01/C02 compiler / export probe broad handler 仍保留未动。
- **下一优先级热点**：优先继续 Phase B.B3，在 `train/models.py` 内建立 skeleton / weight-cache cluster（先做分区与顺序整理，再决定是否补最小 cluster smoke test）；Category C 继续留到 compiler/export 专项。

### Implementation Update — 2026-04-23 Phase B B3 skeleton / weight-cache small batch

- **本轮目标**：继续 Phase B，但只做 `train/models.py` 单文件内的 skeleton / weight-cache cluster 小批次准备；本 batch 仅为 `MotionJointLoss` 的 `set_bone_names`–`_rot_local_tail_candidates` 邻接区域补 future cluster 边界，并锁定 `set_skeleton` / `_resolve_direct_group_masks` / `_joint_weight_vector` / `_parent_relative_matrices` / `_root_relative` 的现有语义；不处理 Category C，不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑。
- **实际完成项**：`train/models.py` 在 skeleton / weight-cache helpers 起点新增 `# === future: train/loss/skeleton_weights.py ===` 分区注释，明确 B3 in-file cluster 边界；未改任何 loss 数值逻辑、默认超参、stats key 或 checkpoint contract。新增 focused regression 覆盖 direct-group root/overlap masking、`_joint_weight_vector` cache hit / invalidate / recompute 等价性，以及 `_parent_relative_matrices` / `_root_relative` 的旋转相对化语义。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `3` 个 skeleton-cluster regression tests：`test_motion_joint_loss_skeleton_cluster_direct_group_masks_exclude_root_and_overlaps`、`test_motion_joint_loss_skeleton_cluster_weight_cache_invalidation_preserves_values`、`test_motion_joint_loss_skeleton_cluster_relative_rotation_helpers`；`tests.train.test_train_models_failfast` 总数增至 `94`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '4840,4852p' train/models.py; sed -n '1780,1870p' tests/train/test_train_models_failfast.py; sed -n '195,207p' docs/changes/2026-04-21_train_models_fail_fast_inventory.md; sed -n '158,159p' docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md; sed -n '587,599p' docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md) | if rg ...`.
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `100` 个用例通过（`tests.train.test_train_models_failfast=94`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮仅完成 B3 的分区边界与语义锁定，不涉及更大范围的方法重排；B3 仍剩 `_resolve_limb_masks` / `_collect_limb_local_stats` / `_compute_unified_weights_cpu` / `_rot_local_tail_scores` / `_rot_local_tail_candidates` 的更完整顺序整理与 cluster smoke；Category C 仍未动。
- **下一优先级热点**：继续 Phase B.B3，把 skeleton / weight-cache cluster 的余下方法按“state update → mask resolution → weight compute → tail helpers → FK relative”顺序整理到同一块；完成后再进入 B4 applicators cluster。

### Implementation Update — 2026-04-23 Phase B B3 skeleton / weight-cache cluster completion

- **本轮目标**：完成 Phase B.B3 剩余 in-file 顺序整理，把 skeleton / weight-cache cluster 按“state update / cache invalidation → mask resolution / skeleton stats → weight computation → tail-risk selection → FK-relative rotation views”整理成连续块；继续不处理 Category C，不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑。
- **实际完成项**：`train/models.py` 中 B3 cluster 已完成顺序整理：`set_bone_names` / `set_skeleton` / `_invalidate_weight_cache` 放在 state update 区；`_resolve_named_joint_indices` / `_resolve_limb_masks` / `_resolve_direct_group_masks` / `_collect_limb_local_stats` 放在 mask/stats 区；`_joint_weight_vector` / `_compute_unified_weights_cpu` 放在 weight 区；`_rot_local_tail_scores` / `_rot_local_tail_candidates` 放在 tail-risk 区；`_parent_relative_matrices` / `_root_relative` 放在 FK-relative 区。函数体语义未改，loss 数值、默认超参、stats key、checkpoint contract 均保持不变。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `2` 个 B3 completion regression tests：`test_motion_joint_loss_skeleton_cluster_limb_stats_helpers`、`test_motion_joint_loss_skeleton_cluster_tail_candidates_and_scores`；`tests.train.test_train_models_failfast` 总数增至 `96`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '4840,5245p' train/models.py; sed -n '1790,1925p' tests/train/test_train_models_failfast.py) | if rg ...`.
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `102` 个用例通过（`tests.train.test_train_models_failfast=96`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：B3 当前只做 single-file cluster / mechanical reordering，没有抽文件，也没有把 stateful helpers free-function 化；后续若做 Phase E 迁移仍需保持 `MotionJointLoss` 私有调用点与 `train/posttrain.py` 侧调用面同步。
- **下一优先级热点**：进入 Phase B.B4 applicators cluster，在 `train/models.py` 内为 `_apply_rot_ortho_component` / `_apply_rot_local_tail_component` / `_apply_rot_local_component` / `_apply_root_velocity_components` / `_apply_motion_components` / `_apply_contact_plan_component` / `_apply_event_clock_components` / `_apply_contact_meas_component` / `_apply_omega_l2_component` / `_apply_aux_components` 建立 `# === future: train/loss/components.py ===` 分区并补最小 stats/key regression；Category C 继续留到 compiler/export 专项。

### Implementation Update — 2026-04-23 Phase B B4 applicators cluster first batch

- **本轮目标**：进入 Phase B.B4，但只做 applicators cluster 的首个小批次：在 `train/models.py` 内建立 `# === future: train/loss/components.py ===` 分区边界，并补最小 forward-level stats/key regression；不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑，不处理 Category C。
- **实际完成项**：`train/models.py` 在 `_apply_rot_ortho_component` 前新增 `# === future: train/loss/components.py ===`，明确 component applicators 的 future split 边界；未移动或改写 applicator 函数体。新增回归通过真实 `MotionJointLoss.forward(...)` 同时触发 rot ortho / rot local / root velocity / direct pose / contact plan / event clock / contact meas / omega L2 applicators，锁定关键 stats key 与 root velocity 数值。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `1` 个 B4 regression test：`test_motion_joint_loss_applicator_cluster_forward_stats_contract`；`tests.train.test_train_models_failfast` 总数增至 `97`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5560,5576p' train/models.py; sed -n '1920,1995p' tests/train/test_train_models_failfast.py) | if rg ...`.
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `103` 个用例通过（`tests.train.test_train_models_failfast=97`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：B4 当前仍与 direct-pose payload helpers 物理相邻；这是刻意保留的单文件边界，后续应由 Phase C.loss 单独收口 direct-pose loss cluster，而不是在 B4 中混合做语义重排。
- **下一优先级热点**：Phase B 下一步建议进入 B5 rot6d cluster；Category C 继续留到 compiler/export 专项。

### Implementation Update — 2026-04-23 Phase B B4 applicators cluster completion

- **本轮目标**：完成 Phase B.B4，把 applicators cluster 明确拆成 motion applicators / direct-pose payload+applicator / auxiliary applicators 三个 in-file 子区，并补一条默认 direct-pose stats regression；不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑，不处理 Category C。
- **实际完成项**：`train/models.py` 的 applicators 区现在显式分成 `Motion applicators.`、`Direct-pose payload helpers stay colocated until Phase C.loss.`、`Direct-pose applicator.`、`Auxiliary applicators.` 四个连续子区；未移动 helper 出文件，也未改任何 applicator 数值逻辑。新增回归锁定：`pred_motion` 非 dict 时，`_apply_direct_pose_component` 仍写入完整 direct-pose default stats 集合且总 loss 为 `0.0`。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `1` 个 B4 completion regression test：`test_motion_joint_loss_applicator_cluster_direct_pose_defaults_when_pred_not_dict`；`tests.train.test_train_models_failfast` 总数增至 `98`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5564,5574p' train/models.py; sed -n '1924,2030p' tests/train/test_train_models_failfast.py) | if rg ...`.
- **验证结果**：通过；联合 `unittest` 共 `104` 个用例通过（`tests.train.test_train_models_failfast=98`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：B4 已完成 in-file cluster 目标，但 direct-pose payload helpers 仍保留在同一文件内；后续如继续收口，必须在 Phase C.loss 中单独处理 payload typing / stats contract，避免把结构调整和语义调整绑定。
- **下一优先级热点**：进入 Phase B.B5 rot6d cluster，按 `_maybe_get_rot6d` → `_denorm_rot6d_flat` → `_extract_rot6d_flat` → `_extract_rot6d_mats` / `_rot6d_matrices` → `compute_rot6d_ortho_loss` → `compute_rot6d_geo_loss` → `compute_rot6d_log_loss` 的顺序做 in-file cluster 化；Category C 继续留到 compiler/export 专项。

### Implementation Update — 2026-04-23 Phase B B5 rot6d cluster completion

- **本轮目标**：完成 Phase B.B5，在 `train/models.py` 内建立 rot6d helper cluster，按 `_maybe_get_rot6d` → `_denorm_rot6d_flat` → `_extract_rot6d_flat` → `_extract_rot6d_mats` / `_rot6d_matrices` → `compute_rot6d_ortho_loss` → `compute_rot6d_geo_loss` → `compute_rot6d_log_loss` 的顺序整理；不做跨文件拆分，不改数学定义，不处理 Category C。
- **实际完成项**：`train/models.py` 已在 rot6d helpers 起点新增 `# === future: train/loss_rot6d.py ===` 与 `Rot6D slice / denorm / matrix helpers.` / `Rot6D objective helpers.` 子区注释；rot6d 相关方法已按 roadmap 顺序重排为 `_maybe_get_rot6d` → `_denorm_rot6d_flat` → `_extract_rot6d_flat` → `_extract_rot6d_mats` → `_rot6d_matrices` → `compute_rot6d_ortho_loss` → `compute_rot6d_geo_loss` → `compute_rot6d_log_loss`。函数体与数值语义未改。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `2` 个 B5 regression tests：`test_motion_joint_loss_rot6d_cluster_extract_and_matrix_helpers`、`test_motion_joint_loss_rot6d_cluster_objective_helpers`；`tests.train.test_train_models_failfast` 总数增至 `100`。
- **当前统计**：`train/models.py` broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5338,5518p' train/models.py; sed -n '2020,2088p' tests/train/test_train_models_failfast.py) | if rg ...`.
- **验证结果**：通过；联合 `unittest` 共 `106` 个用例通过（`tests.train.test_train_models_failfast=100`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：Phase B 已完成 B1/B3/B4/B5，但 `_warn_once` 仍按 roadmap 保持冻结；rot6d cluster 仍留在单文件内，未做 `_Rot6DLossHelpers` 类化或 `_joint_weight_vector` 外部化，这些都明确留到 Phase E。
- **下一优先级热点**：若继续单文件收口，下一步应进入 Phase C.loss，单独整理 direct-pose loss cluster 与 payload typing；Category C compiler/export probes 继续留到专项处理。

---

## Category A — should become fail-fast first

> 这些条目在 direct-pose / forward / `MotionJointLoss` 主路径上吞结构错误，或在 build-time 静默禁用关键 head。类别 A 已按优先级排序。

| # | Line | Function / method | Hotspot | Context | Swallowed risk | Why A | Suggested handling |
|---:|---:|---|---|---|---|---|---|
| A01 | `3653` | `EventMotionModel.forward` | direct-pose / forward | Non-side `direct_pose_leg_head` branch catches the whole leg residual application. | Missing head, bad rot6d slice, bad joint index, shape mismatch, or SO(3) gate/scale bug becomes `pass`; `out_direct` continues without leg correction. | Highest-risk direct-pose main path; silent branch skip changes prediction semantics. | Replace with explicit preconditions and `RuntimeError` / `ValueError` carrying branch name, expected shapes, and relevant config attrs. |
| A02 | `3546` | `EventMotionModel.forward` | direct-pose / forward | Side-routing direct-pose leg branch catches the whole routed leg head block. | Side routing, phase, plan-other, cue, embedding, sign gate, or shared gate failures are swallowed. | Entire direct-pose side path can disappear while forward still succeeds. | Fail-fast around routed leg branch; validate side positions, input dims, and head presence before execution. |
| A03 | `3501` | `EventMotionModel.forward` | direct-pose / forward | Shared side-routing learned gate falls back to ungated `omega_leg`. | Missing `direct_pose_leg_gate_head_shared` or bad gate output silently disables learned gating. | Gate is part of direct-pose correction semantics, not a diagnostic. | Raise when gate mode is `learned` and head/output shape is invalid. |
| A04 | `3539` | `EventMotionModel.forward` | direct-pose / forward | Shared side-routing scale gate falls back to unscaled `omega_leg`. | Missing scale head or invalid output silently disables learned scale. | Scale controls correction magnitude; fallback changes learned behavior. | Raise when gate mode is `scale` and scale head/output shape is invalid. |
| A05 | `3614` | `EventMotionModel.forward` | direct-pose / forward | Non-side learned gate falls back to ungated `omega_leg`. | Gate head absence or shape mismatch is hidden. | Same semantic path as A03 but for non-side routing. | Raise with expected `(B,T,K)` gate shape and current branch metadata. |
| A06 | `3637` | `EventMotionModel.forward` | direct-pose / forward | Non-side scale gate falls back to unscaled `omega_leg`. | Scale head absence, invalid log-mag shape, or numerical error is hidden. | Same semantic path as A04 but for non-side routing. | Raise with expected `(B,T,K)` scale shape and gate mode. |
| A07 | `3450` | `EventMotionModel.forward` | direct-pose / forward | Side sign gate catches and keeps `direct_leg_side_sign_gate=None`. | Broken side sign gate silently removes side-specific sign modulation. | Direct-pose branch output changes without any observable failure. | Validate sign gate output shape and raise if enabled but unusable. |
| A08 | `3294` | `EventMotionModel.forward` | direct-pose / forward | Per-side phase view failure replaces phase inputs with zeros. | Malformed `phase_z_in_direct` silently becomes no phase signal. | Direct-pose side head consumes false-zero phase context. | Validate `phase_z` shape before view; raise expected `(B,T,2*C)` contract error. |
| A09 | `3383` | `EventMotionModel.forward` | direct-pose / forward | Side embedding failure drops both side embeddings. | Embedding device/dtype/module errors silently disable side identity features. | Enabled module should either work or fail; fallback changes feature contract. | Raise when `direct_pose_leg_side_embed` exists but cannot produce `(B,T,D)`. |
| A10 | `3137` | `EventMotionModel.forward` | direct-pose / forward | Direct-pose time PE concat failure is ignored. | Time feature dim/device mismatch silently removes time context from direct head. | Direct-pose feature contract is corrupted while output still exists. | Validate feature dims and raise if time PE is enabled but cannot concatenate. |
| A11 | `3122` | `EventMotionModel.forward` | direct-pose / forward | `direct_pose_feat_source` conversion failure falls back to `cond`. | Invalid runtime attr/config silently changes direct-pose feature source. | Feature source is a core direct-pose contract, not an optional stat. | Normalize and validate in init/runtime controls; raise on invalid type/value. |
| A12 | `2406` | `EventMotionModel.forward._expand_state_sequence` | direct-pose / forward | Sequence expansion failure returns `None`. | `phase_z` / `phase_event_age` shape errors silently remove phase or side-cue inputs. | Roadmap already called this out; direct-pose input contract is hidden. | Raise with feature name and expected `(B,T,D)` / broadcastable shapes. |
| A13 | `2513` | `EventMotionModel.forward` | forward | Contact-plan observed init head failure falls back to `None`, then learnable/zero init. | Bad obs feature shape, init head shape, or device mismatch silently changes initial `plan_z`. | Contact-plan initial state affects forward semantics. | Fail-fast when `init_mode` requests obs and obs init fails; keep explicit zero only for `init_mode=zeros`. |
| A14 | `2572` | `EventMotionModel.forward` | forward | Time grid construction failure disables `t_grid`. | Bad `time_index` shape silently disables contact/direct time PE. | Forward input contract failure becomes feature removal. | Raise when time features are requested and `time_index` cannot be normalized. |
| A15 | `2587` | `EventMotionModel.forward` | forward | Contact-plan time PE construction failure sets `time_pe=None`. | Invalid PE dimension/base/time grid silently disables time bias. | Enabled time head should have valid PE input. | Raise with PE dim, half dim, and `t_grid` shape. |
| A16 | `2601` | `EventMotionModel.forward` | direct-pose / forward | Direct-pose time PE construction failure sets `time_pe_direct=None`. | Invalid direct time PE silently removes direct-pose time context. | Direct-pose feature path changes silently. | Raise with direct PE dim and `t_grid` shape. |
| A17 | `2664` | `EventMotionModel.forward` | forward | Adaptive history module failure is swallowed. | Module/device/shape errors leave `pose_history` unprocessed. | Enabled adaptive-history runtime must be observable if broken. | Fail-fast if `adaptive_history_module` exists and processing fails. |
| A18 | `2708` | `EventMotionModel.forward` | forward | Frozen encoder period feature failure returns `period_feat=None`. | Broken frozen encoder / period head silently removes event-clock period signal. | Configured model component fails but forward semantics continue. | Raise when frozen encoder and period head are both configured but unusable. |
| A19 | `2721` | `EventMotionModel.forward` | direct-pose / forward | Event-clock branch phase input append failure is ignored. | Per-step phase sequence can be truncated/missing without error. | Direct-pose phase path becomes inconsistent across time. | Raise with `_t`, expected phase dim, and actual source shape. |
| A20 | `2730` | `EventMotionModel.forward` | direct-pose / forward | Event-clock branch side-cue append failure is ignored. | Side cue sequence can silently miss steps. | Direct-pose routed leg input contract breaks silently. | Raise with cue mode, `_t`, and expected contact dim. |
| A21 | `2767` | `EventMotionModel.forward` | forward | Event-clock time bias failure sets `time_term=None`. | Contact-plan time head errors silently remove time bias. | Enabled time head output should be required. | Raise when `contact_plan_time_head` is enabled and call fails. |
| A22 | `2810` | `EventMotionModel.forward` | direct-pose / forward | Non-event-clock phase append failure is ignored. | Same phase-sequence risk as A19 without event-clock. | Direct-pose phase input is silently malformed. | Raise with branch marker `event_clock=off`. |
| A23 | `2819` | `EventMotionModel.forward` | direct-pose / forward | Non-event-clock side-cue append failure is ignored. | Same side-cue risk as A20 without event-clock. | Direct-pose side-cue input is silently malformed. | Raise with branch marker `event_clock=off`. |
| A24 | `2830` | `EventMotionModel.forward` | forward | Non-event-clock contact-plan time term failure sets `time_term=None`. | Time bias is silently dropped. | Enabled contact-plan time head should not fail quietly. | Raise when time head exists and time PE is provided. |
| A25 | `2847` | `EventMotionModel.forward` | direct-pose / forward | `phase_in_direct_seq` stack failure sets `phase_z_in_direct=None`. | Inconsistent per-step phase tensors become missing phase input. | Direct-pose phase contract should be explicit. | Validate list element shapes before stack; raise on mismatch. |
| A26 | `2852` | `EventMotionModel.forward` | direct-pose / forward | `leg_side_cue_seq` stack failure sets `leg_side_cue_in=None`. | Inconsistent side-cue tensors become missing cue input. | Direct-pose side routing contract should be explicit. | Validate list element shapes before stack; raise on mismatch. |
| A27 | `2856` | `EventMotionModel.forward` | forward / loss signal | `plan_logits` stack failure sets `contacts_plan_logits=None`. | Contact-plan logits may disappear from outputs and downstream loss/stats. | `contacts_plan_logits` is consumed later by loss code. | Raise on shape mismatch unless explicitly in debug-only mode. |
| A28 | `3740` | `EventMotionModel.forward` | forward | Lambda-fusion rollout-step normalization failure returns zero step feature. | Bad `rollout_step` silently becomes step `0`. | Lambda-fusion can receive wrong conditioning. | Raise when `lambda_fusion_use_rollout_step` is enabled and shape is invalid. |
| A29 | `3755` | `EventMotionModel.forward` | forward / loss signal | Whole lambda-fusion head block is swallowed. | Head shape/device errors remove `lambda_fusion` outputs and any dependent loss. | Learned fusion output disappears silently. | Fail-fast when head exists; validate output dims before storing. |
| A30 | `3685` | `EventMotionModel.forward` | direct-pose / forward | Outer direct-pose block catches broad `Exception as exc` and re-raises `RuntimeError`. | It does not swallow, but broad wrapping can obscure exact failure class and duplicates inner broad catches. | Already fail-fast, but still a broad handler in the main path. | After inner branches are cleaned, keep contextual raise only if needed; otherwise let typed errors propagate. |
| A31 | `1533` | `EventMotionModel._build_lambda_and_so3_aux_heads` | build / rot6d | Lambda-fusion rot6d slice inference failure sets joint count `0`. | Layout/config error silently disables lambda-fusion head. | Roadmap explicitly lists this as fail-fast candidate. | Raise with `output_layout`, `out_motion_dim`, and expected rot6d slice. |
| A32 | `1568` | `EventMotionModel._build_lambda_and_so3_aux_heads` | build / rot6d | SO(3) corrector rot6d slice inference failure sets joint count `0`. | Layout/config error silently disables SO(3) corrector. | Same rot6d contract as A31. | Raise with layout metadata instead of disabling head. |
| A33 | `1556` | `EventMotionModel._build_lambda_and_so3_aux_heads` | build | Lambda-fusion head final layer init failure is ignored. | Deterministic zero/logit init can fail and leave random init. | Init semantics affect reproducibility and baseline snapshots. | Raise if final layer is not init-able as configured. |
| A34 | `1588` | `EventMotionModel._build_lambda_and_so3_aux_heads` | build | SO(3) corrector final layer init failure is ignored. | Deterministic zero init can fail and leave random correction. | Same reproducibility risk as A33. | Raise if final layer init fails. |
| A35 | `1190` | `EventMotionModel._build_contact_plan_modules` | build / forward | Contact-plan init head final layer zero-init failure is ignored. | Initial plan state path can become randomly initialized. | Deterministic init is part of current behavior. | Raise with module path `contact_plan_init_head[-1]`. |
| A36 | `1206` | `EventMotionModel._build_contact_plan_modules` | build / forward | Contact-plan time head zero-init failure is ignored. | Time-bias head can start nonzero unexpectedly. | Deterministic init failure should be visible. | Raise with module path `contact_plan_time_head`. |
| A37 | `980` | `EventMotionModel._init_direct_pose_routing_metadata` | direct-pose build | `direct_pose_leg_joint_idx_tensor` register failure is ignored. | Leg joint index tensor may be absent while later runtime expects it. | Direct-pose routing metadata is structural. | Raise on register-buffer failure; do not continue with partial metadata. |
| A38 | `1019` | `EventMotionModel._init_direct_pose_routing_metadata` | direct-pose build | Split leg index tensor update/register failure is ignored. | Split/routed leg metadata can be partially updated. | Partial direct-pose metadata causes delayed failures. | Fail-fast with joint index list and tensor shape. |
| A39 | `1144` | `EventMotionModel._init_direct_pose_routing_metadata` | direct-pose build | Side position buffer registration failure is ignored. | Side-routing tensors may be missing while side-routing remains enabled. | Direct-pose side routing contract is incomplete. | Raise if side-routing buffers cannot be registered. |
| A40 | `1151` | `EventMotionModel._init_direct_pose_routing_metadata` | direct-pose build | Side embedding weight zero-init failure is ignored. | Side embedding may keep random nonzero init. | Deterministic direct-pose side feature init should be observable. | Raise on init failure when embedding exists. |
| A41 | `3852` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | `direct_pose_loss_arm_weight` conversion failure falls back to `1.0`. | Invalid loss weight silently changes loss recipe. | Roadmap explicitly lists this region as category A. | Validate scalar in loss config and raise on invalid type/value. |
| A42 | `3856` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | `direct_pose_loss_else_weight` conversion failure falls back to `1.0`. | Invalid non-arm weight silently changes loss recipe. | Same direct-pose loss contract as A41. | Validate scalar and raise. |
| A43 | `3870` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | Group-norm EMA beta conversion failure falls back to `0.9`. | Invalid EMA beta silently changes balancing dynamics. | Loss balancing state must be explicit. | Validate beta range/type and raise. |
| A44 | `3874` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | Group-norm ratio min conversion failure falls back to `0.2`. | Invalid clamp lower bound silently changes group balance. | Direct-pose loss weighting contract. | Validate scalar and raise. |
| A45 | `3878` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | Group-norm ratio max conversion failure falls back to `5.0`. | Invalid clamp upper bound silently changes group balance. | Direct-pose loss weighting contract. | Validate scalar and raise. |
| A46 | `3887` | `MotionJointLoss.__init__` | MotionJointLoss / direct-pose loss | Group-norm epsilon conversion failure falls back to `1e-6`. | Invalid epsilon silently changes numerical behavior. | Loss numerical contract should be explicit. | Validate positive finite scalar and raise. |
| A47 | `3947` | `MotionJointLoss.__init__` | MotionJointLoss / skeleton | Skeleton `ref_local_offsets_m` tensor conversion failure sets `bone_offsets=None`. | FK / skeleton-aware losses can silently degrade. | Roadmap explicitly lists this region as category A. | Raise when skeleton provides offsets but they cannot be converted. |
| A48 | `4091` | `MotionJointLoss.set_skeleton` | MotionJointLoss / skeleton | Runtime skeleton offsets conversion failure sets `bone_offsets=None`. | Updating skeleton can silently drop offsets and invalidate cached assumptions. | Same skeleton contract as A47. | Raise on invalid offsets; clear caches only after successful validation. |
| A49 | `4988` | `MotionJointLoss._compute_direct_pose_group_base_payload` | MotionJointLoss / direct-pose loss | Direct-pose payload `arm_w` conversion failure falls back to `1.0`. | Per-call override can silently change direct-pose group weighting. | Loss payload is a main direct-pose path. | Validate override scalar before payload computation and raise. |
| A50 | `4992` | `MotionJointLoss._compute_direct_pose_group_base_payload` | MotionJointLoss / direct-pose loss | Direct-pose payload `else_w` conversion failure falls back to `1.0`. | Per-call override can silently change non-arm weighting. | Same direct-pose loss payload risk as A49. | Validate override scalar and raise. |
| A51 | `5059` | `MotionJointLoss._compute_direct_pose_group_norm_shared._resolve_scalar` | MotionJointLoss / direct-pose loss | Group-norm scalar resolver catches everything and falls back. | Bad runtime override silently changes EMA/bounds/epsilon behavior. | Central scalar resolver affects multiple direct-pose loss controls. | Replace with typed scalar resolver that raises for invalid user/runtime values. |

---

## Category B — narrow exception plus explicit fallback

> 这些条目当前有明确 fallback 意图，但 broad catch 过宽。下一轮应改成具体异常类型、显式 fallback 值和局部注释/变量名，不要新增 silent debt。

| # | Line | Function / method | Context | Swallowed risk | Why B | Suggested handling |
|---:|---:|---|---|---|---|---|
| B01 | `654` | `EventMotionModel.__init__` | `direct_pose_phase_z_mode` string normalization falls back to `"concat"`. | Non-string/custom object errors become default mode. | Config normalization with explicit default. | Handle `None` directly; narrow invalid conversion to `TypeError` / `ValueError` or validate and raise if defaults should not apply. |
| B02 | `719` | `EventMotionModel.__init__` | `direct_pose_leg_mode` normalization falls back to `"rot6d_add"`. | Invalid mode object becomes additive path. | Explicit mode default exists, but catch is too broad. | Normalize only strings / `None`; raise for unsupported non-string objects. |
| B03 | `730` | `EventMotionModel.__init__` | `direct_pose_leg_max_deg` float parse falls back to `0.0`. | Invalid scalar disables max-angle clamp. | Numeric config fallback is explicit. | Catch `TypeError` / `ValueError`; keep finite/range validation after parse. |
| B04 | `737` | `EventMotionModel.__init__` | `direct_pose_leg_gate_mode` normalization falls back to `"none"`. | Invalid mode disables gate. | Mode fallback exists but should be typed. | Narrow parse exceptions; decide whether unknown strings stay `"none"` or become `ValueError`. |
| B05 | `750` | `EventMotionModel.__init__` | `direct_pose_leg_gate_power` parse falls back to `1.0`. | Invalid power removes nonlinear gate shaping. | Numeric fallback is explicit. | Catch scalar parse exceptions only; keep positive finite validation. |
| B06 | `757` | `EventMotionModel.__init__` | `direct_pose_leg_scale_log_clip` parse falls back to `4.0`. | Invalid clip changes scale clamp. | Numeric fallback is explicit. | Catch scalar parse exceptions only. |
| B07 | `764` | `EventMotionModel.__init__` | `direct_pose_leg_scale_clamp_k` parse falls back to `0.0`. | Invalid clamp disables scale clamp. | Numeric fallback is explicit. | Catch scalar parse exceptions only. |
| B08 | `777` | `EventMotionModel.__init__` | `direct_pose_leg_contact_order` normalization falls back to `"lr"`. | Invalid object changes left/right contact mapping to default. | Mode has explicit dataset default. | Narrow parsing and raise for unsupported non-string values if caller provided one. |
| B09 | `788` | `EventMotionModel.__init__` | `direct_pose_leg_side_embed_dim` int parse falls back to `0`. | Invalid scalar disables side embedding. | Optional feature default is explicit. | Catch `TypeError` / `ValueError`; range clamp remains explicit. |
| B10 | `811` | `EventMotionModel.__init__` | `direct_pose_leg_side_cue` normalization falls back to `"none"`. | Invalid cue object disables side cue. | Optional cue mode default is explicit. | Narrow parse exceptions and validate accepted strings. |
| B11 | `824` | `EventMotionModel.__init__` | `direct_pose_leg_side_cue_tau` parse falls back to `0.0`, then normalizes to `30.0`. | Invalid tau silently chooses default smoothing. | Numeric fallback is explicit. | Catch scalar parse exceptions; simplify fallback so default value is stated once. |
| B12 | `939` | `EventMotionModel.__init__` | `_init_bone_residual_adapters(...)` failure disables adapters. | Metadata mismatch and real module construction bugs are both hidden. | Comment indicates optional adapter fallback, but only metadata errors should be allowed. | Narrow to expected metadata exceptions (`KeyError`, `IndexError`, `TypeError`, `ValueError`) and fail-fast unexpected module errors. |
| B13 | `1011` | `EventMotionModel._init_direct_pose_routing_metadata` | Bone names lookup failure leaves `direct_pose_leg_joint_names` empty. | Bad `bone_names` shape/type hides naming metadata errors. | Names are metadata; tensor routing is handled separately. | Catch `TypeError` / `IndexError` only; keep explicit empty-name fallback. |
| B14 | `1952` | `EventMotionModel._normalize_eval_runtime_controls` | `contact_plan_inject_scale` parse falls back to `1.0`. | Bad runtime scalar silently removes scale override. | Eval/runtime control scalar has clear default. | Catch `TypeError` / `ValueError`; preserve explicit default. |
| B15 | `1956` | `EventMotionModel._normalize_eval_runtime_controls` | `contact_plan_time_bias_scale` parse falls back to `1.0`. | Bad runtime scalar silently removes time-bias override. | Eval/runtime control scalar has clear default. | Catch `TypeError` / `ValueError`; preserve explicit default. |
| B16 | `2029` | `EventMotionModel._finalize_contact_plan_debug_logits._stack` | Debug-logit stack returns `None` on failure. | Shape mismatch in debug tensors loses diagnostic output. | Diagnostic path, not forward core. | Catch `RuntimeError` from `torch.stack` only or pre-validate shapes. |
| B17 | `2242` | `EventMotionModel._compute_direct_pose_leg_cross_leg_ablation` | Ablation helper `contact_dim` parse falls back to `0`. | Bad attr disables ablation analysis. | Runtime ablation/debug path, not default training semantics. | Catch scalar parse exceptions only; make "skip ablation" explicit. |
| B18 | `2246` | `EventMotionModel._compute_direct_pose_leg_cross_leg_ablation` | Ablation helper joint-name lookup falls back to `[]`. | Bad metadata disables cross-leg ablation analysis. | Runtime ablation/debug path. | Catch `TypeError` / `ValueError`; keep explicit skip. |
| B19 | `4446` | `MotionJointLoss.compute_attention_regularization` | Geomask direct broadcast failure switches to alternate geomask handling. | Real tensor shape bug may be hidden as fallback regularization. | Auxiliary attention regularization supports multiple geomask shapes. | Narrow to expected `RuntimeError` from shape mismatch and validate accepted ranks (`2`, `3`, `4`). |

---

## Category C — leave unchanged in this round

> 这些是 compiler/export / third-party boundary probes. 本轮不改，避免引入 export 行为漂移；后续只在专项清理时收窄。

| # | Line | Function / method | Context | Swallowed risk | Why C | Suggested handling |
|---:|---:|---|---|---|---|---|
| C01 | `2913` | `EventMotionModel.forward` | `torch._dynamo.is_compiling()` probe controls finite guards. | Private API absence/runtime failure leaves `_skip_guard=False`. | Compiler boundary; changing now could affect export/compile behavior. | Leave for now; later catch `AttributeError` / documented runtime failures only. |
| C02 | `2917` | `EventMotionModel.forward` | `torch.onnx.is_in_onnx_export()` probe controls finite guards. | ONNX API absence/runtime failure leaves `_skip_guard` unchanged. | Export boundary; changing now could affect ONNX path. | Leave for now; later catch `AttributeError` / documented runtime failures only. |

---

## Verification Notes

- Inventory source of truth: AST scan of `train/models.py` for `ExceptHandler(type=Name("Exception"))`, plus regex check for exact `except Exception:` and `except Exception as exc:`.
- Static count result: exact `except Exception:` = `71`; broad `Exception` handlers including `as exc` = `72`.
- No helper script was written to disk; analysis used inline shell/Python commands only.
- This document intentionally does not prescribe code changes beyond next-step handling; Phase A coding should apply this inventory without mixing in Phase B/C/D refactors.

### Implementation Update — 2026-04-23 Phase C.loss.1 direct-pose loss cluster prep

- **本轮目标**：进入 Phase C.loss，但只做 C.loss.1 的 single-file cluster 收口准备；范围限定在 `MotionJointLoss` 的 direct-pose loss helpers，不处理 C01/C02，不做跨文件拆分，不改 `EventMotionModel.forward` 主逻辑，不改 loss 数值语义。
- **实际完成项**：`train/models.py` 已在 direct-pose loss 区新增 `# === future: train/loss/direct_pose.py ===` 分区注释，并补上 `Direct-pose default stats and pair normalization.`、`Direct-pose group payload builders.`、`Direct-pose payload assembly.`、`Direct-pose component applicator.` 子区注释；`_direct_pose_default_stats` / `_direct_pose_extra_defaults` / `_prepare_direct_pose_pair` / `_compute_direct_pose_group_norm_payload` / `_compute_direct_pose_group_base_payload` / `_compute_direct_pose_group_norm_shared` / `_compute_direct_pose_payload` / `_apply_direct_pose_component` 继续保持连续 in-file cluster，函数体和 loss 数值语义未改。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `4` 个 C.loss.1 regression tests：`test_motion_joint_loss_direct_pose_default_stats_key_contract`、`test_motion_joint_loss_prepare_direct_pose_pair_normalizes_2d_3d_inputs`、`test_motion_joint_loss_group_base_payload_arm_else_balance_contract`、`test_motion_joint_loss_group_norm_shared_can_skip_ema_update`；`tests.train.test_train_models_failfast` 总数增至 `104`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `110`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5760,6168p' train/models.py; sed -n '2000,2170p' tests/train/test_train_models_failfast.py) | rg -n \"\\.get\\(.*\\.get\\(|warnings?\\.warn\\(|state_dict\\[.*\\]\\s*=\\s*state_dict\\.pop\\(|# .* compat|# .* legacy\"`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `110` 个用例通过（`tests.train.test_train_models_failfast=104`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，本轮 touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮只完成 C.loss.1 的 cluster 边界和 contract smoke，尚未进入 C.loss.2 的 payload dataclass typing，也未冻结 C.loss.3 stats key contract 表；Category C 的 C01/C02 compiler / export probe broad handler 仍保留。
- **下一优先级热点**：优先进入 C.loss.2，把 `_compute_direct_pose_payload` 相关 payload 输入/输出类型化为 dataclass，并显式化 EMA / group-norm state 访问；保持现有 stats key、默认超参和 loss 数值语义不变。

### Implementation Update — 2026-04-23 Phase C.loss.2 direct-pose payload typing

- **本轮目标**：完成 C.loss.2；把 direct-pose payload 层类型化成 dataclass，并把 EMA / group-norm state 访问显式化；保持 `train/models.py` 单文件内收口，不做跨文件拆分，不改默认超参、stats key、checkpoint contract、loss 数值语义，也不处理 Category C。
- **实际完成项**：`train/models.py` 新增 `_DirectPosePair`、`_DirectPoseGroupBaseRequest`、`_DirectPoseGroupNormRequest`、`_DirectPoseGroupNormResult`、`_DirectPosePayloadRequest`、`_DirectPosePayloadResult` 六个 internal dataclass，用于 direct-pose pair normalize、group-base payload、group-norm payload、direct-pose payload 的 typed request/result 表达；新增 `_compute_direct_pose_group_norm_from_request(...)`、`_compute_direct_pose_payload_from_request(...)` 两个 typed 内部入口，以及 `_direct_pose_group_norm_ema_snapshot(...)` / `_direct_pose_group_norm_ema_value(...)` / `_store_direct_pose_group_norm_ema(...)` 三个 EMA state helper。对外保留 `_compute_direct_pose_group_base_payload(...)`、`_compute_direct_pose_group_norm_shared(...)`、`_compute_direct_pose_payload(...)` 的既有 dict / tuple 返回契约，`train/posttrain.py` 现有调用面无需联动。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `2 -> 2`；精确 `except Exception:` 维持 `2 -> 2`；`except Exception as exc` 维持 `0 -> 0`；剩余 broad handler 仍仅为 Category C `2`（C01–C02）。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `2` 个 C.loss.2 regression tests：`test_motion_joint_loss_direct_pose_payload_request_result_types`、`test_motion_joint_loss_group_norm_request_result_types`；并把 `_prepare_direct_pose_pair` 回归改为验证 dataclass field 访问。`tests.train.test_train_models_failfast` 总数增至 `106`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `112`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '4488,4568p' train/models.py; sed -n '4568,4658p' train/models.py; sed -n '5760,6175p' train/models.py; sed -n '2030,2325p' tests/train/test_train_models_failfast.py) | rg -n \"\\.get\\(.*\\.get\\(|warnings?\\.warn\\(|state_dict\\[.*\\]\\s*=\\s*state_dict\\.pop\\(|# .* compat|# .* legacy\"`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `112` 个用例通过（`tests.train.test_train_models_failfast=106`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`2` / exact=`2` / as_exc=`0`，本轮 touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮已完成 C.loss.2，但 C.loss.3 的 stats key contract 表仍未单独冻结；typed dataclass 当前仍为 `train/models.py` 内部实现细节，后续跨文件迁移时要保持 public tuple/dict helper 契约不漂移；Category C 的 C01/C02 compiler / export probe broad handler 仍保留。
- **下一优先级热点**：进入 C.loss.3，产出 direct-pose stats key contract 表，并用最小 regression 锁定 `_apply_direct_pose_component` 最终写入集合；Category C 继续留到 compiler/export 专项。

### Implementation Update — 2026-04-23 Phase C.loss.3 stats contract + Category C probe cleanup

- **本轮目标**：一起完成 C.loss.3 与 Category C；冻结 `_apply_direct_pose_component` stats 写入 key contract，并把 `torch._dynamo.is_compiling()` / `torch.onnx.is_in_onnx_export()` 两个 broad compiler/export probe 收窄为显式 helper；保持单文件内完成，不改 loss 数值语义、默认超参、checkpoint contract，不引入 warning-only debt。
- **实际完成项**：`train/models.py` 新增 `_DIRECT_POSE_DEFAULT_STAT_KEYS`、`_DIRECT_POSE_COMPONENT_STAT_KEYS` 两个 module-level contract tuple，并通过 `_direct_pose_default_stat_keys()` / `_direct_pose_component_stat_keys()` 作为当前单文件内 contract 入口；`_apply_direct_pose_component` 的 default-path 与 group-norm path 现均有 exact stats-key regression 锁定。`EventMotionModel.forward(...)` 中的 `_skip_guard` probe 已改为调用 `_torch_dynamo_is_compiling_safe()` 与 `_torch_onnx_is_in_export_safe()`，仅对 `AttributeError` / `RuntimeError` 做显式 fallback，broad `except Exception` 已完全清零。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler `2 -> 0`；精确 `except Exception:` `2 -> 0`；`except Exception as exc` 维持 `0 -> 0`；Category C `2 -> 0`。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `4` 个 regression tests：`test_motion_joint_loss_direct_pose_component_stats_contract_default_path`、`test_motion_joint_loss_direct_pose_component_stats_contract_group_norm_path`、`test_torch_dynamo_probe_safe_handles_missing_and_runtime_failure`、`test_torch_onnx_probe_safe_handles_missing_and_runtime_failure`；并更新 `test_motion_joint_loss_direct_pose_default_stats_key_contract` 使其显式绑定 module-level contract tuple。`tests.train.test_train_models_failfast` 总数增至 `110`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `116`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '56,110p' train/models.py; sed -n '3498,3522p' train/models.py; sed -n '5820,6305p' train/models.py; sed -n '1960,2335p' tests/train/test_train_models_failfast.py) | rg -n \"\\.get\\(.*\\.get\\(|warnings?\\.warn\\(|state_dict\\[.*\\]\\s*=\\s*state_dict\\.pop\\(|# .* compat|# .* legacy\"`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `116` 个用例通过（`tests.train.test_train_models_failfast=110`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`，本轮 touched hunks 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：Phase C.loss 与 Category C 已完成，但 direct-pose stats contract 目前仍以 roadmap 表 + module-level tuple 形式维护；若后续跨文件迁移到 `train/loss/direct_pose.py`，必须原样迁移 contract tuple 并保持现有 public helper 返回契约。`_warn_once` 仍按 roadmap 留在 Phase E。
- **下一优先级热点**：broad handler inventory 已收敛完成；若继续推进，最推荐进入 Phase D，开始 `MotionJointLoss` / `EventMotionModel.forward` 的编排层收口，或单独开启 Phase E 的 `_warn_once` / 跨文件迁移准备。

### Implementation Update — 2026-04-23 Phase D.D2 batch 1 MotionJointLoss orchestration shell

- **本轮目标**：进入 Phase D，但只做 D2 的第一批：在 `train/models.py` 内收口 `MotionJointLoss.forward(...)` 周边编排层；范围限定为 `forward(...)`、`_prepare_forward_inputs(...)`、`_init_loss_group_tracker(...)`、`_accumulate_loss_contrib(...)`、`_loss_group_stats(...)`、`_submit_component_loss(...)`、`_prepare_aux_supervision_pair(...)` 一组，不改 applicator 内部数值语义，不碰 `EventMotionModel.forward(...)` 主逻辑，不做跨文件迁移。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 仅对 `MotionJointLoss` orchestration 壳做机械重排，并新增 `_dispatch_forward_components(...)` 作为薄包装，把 `motion -> direct_pose -> aux` 的 applicator dispatch 顺序显式化。`_prepare_forward_inputs(...)` / `_coerce_forward_base_output(...)` / `_run_forward_base(...)`、`_init_loss_group_tracker(...)` / `_accumulate_loss_contrib(...)` / `_loss_group_stats(...)` / `_prepare_aux_supervision_pair(...)` / `_submit_component_loss(...)` 现已聚拢到同一 orchestration 区，便于后续继续做 D2 批次。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_train_models_failfast` 新增 `4` 个 orchestration regression tests：`test_motion_joint_loss_prepare_forward_inputs_preserves_dict_and_tensor_contract`、`test_motion_joint_loss_prepare_aux_supervision_pair_aligns_steps_dtype_and_device`、`test_motion_joint_loss_submit_component_loss_tracks_group_stats_contract`、`test_motion_joint_loss_forward_dispatch_order_regression`。`tests.train.test_train_models_failfast` 总数增至 `114`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `120`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5635,6685p' train/models.py; sed -n '2240,2490p' tests/train/test_train_models_failfast.py) | rg -n \"\\.get\\(.*\\.get\\(|warnings?\\.warn\\(|state_dict\\[.*\\]\\s*=\\s*state_dict\\.pop\\(|# .* compat|# .* legacy\"`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `120` 个用例通过（`tests.train.test_train_models_failfast=114`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`，本轮 touched ranges 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；Phase D 的 stage6 deterministic smoke / forward output snapshot 仍未在本 batch 复跑，后续做 D1 或更大范围的 D2 重排前建议补齐。
- **下一优先级热点**：inventory 视角下，下一步最值得跟踪的是 D2 后续批次是否继续保持 broad=`0` 且不引入 removal-policy 反模式；若开始 D1 `EventMotionModel.forward(...)`，需要继续沿用本轮的 orchestration shell regression 模式锁住 dispatch 顺序。

### Implementation Update — 2026-04-23 Phase D.D2 batch 2 MotionJointLoss init/skeleton read path

- **本轮目标**：继续 Phase D.D2，但只做第二批：压平 `MotionJointLoss.__init__(...)` 与 skeleton/cache bootstrap 的单文件阅读路径；不改默认超参、checkpoint/output key、loss 数值语义，不碰 `EventMotionModel.forward(...)`，不做跨文件拆分。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 只做 `MotionJointLoss` constructor/read-path 的机械整理。constructor 现在显式分成 retired-key boundary、local scalar normalization、core weights/direct-pose config、layout/rot6d contract、skeleton/cache bootstrap、orchestration tracker defaults。为满足 removal-policy grep，已把 `layout.get('slices')` 同行双调用拆成 `slices_layout` 中间变量，并避免新增 `# legacy` / `# compat` 注释。`_warn_once` 签名和实现保持冻结。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：新增 `test_motion_joint_loss_skeleton_cluster_init_state_contract` 与 `test_motion_joint_loss_finalize_forward_outputs_adds_loss_group_stats`。`tests.train.test_train_models_failfast` 总数增至 `116`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `122`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py && python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '4677,4955p' train/models.py; sed -n '4979,5045p' train/models.py; sed -n '1810,1888p' tests/train/test_train_models_failfast.py; sed -n '2300,2452p' tests/train/test_train_models_failfast.py) | rg -n <removal-policy §6 regex>`。
- **验证结果**：通过；联合 `unittest` 共 `122` 个用例通过（`tests.train.test_train_models_failfast=116`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`，本轮 touched ranges 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；Phase D 的 stage6 deterministic smoke / forward output snapshot 仍未在本 batch 复跑。
- **下一优先级热点**：继续跟踪 D2 后续批次是否保持 broad=`0` 与 removal-policy grep 零命中；如果下一步进入 D1，建议先新增 forward snapshot / dispatch-order smoke 后再整理 `EventMotionModel.forward(...)`。

### Implementation Update — 2026-04-23 Phase D.D2 batch 3 payload/applicator boundary alignment

- **本轮目标**：继续 Phase D.D2，但只做第三批：对齐 `MotionJointLoss` 的 direct-pose payload builders 与 component applicators 的物理边界；保持单文件内完成，不改 loss 数值语义、默认超参、stats key、checkpoint/output key，不碰 `EventMotionModel.forward(...)`。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 仅对 direct-pose payload/applicator 的物理顺序和子区注释做整理。`_compute_direct_pose_group_base_payload(...)` 现在位于 group-norm wrapper 之前，direct-pose cluster 的阅读顺序变为 stats/pair → group base payload → group norm wrapper/EMA → typed group norm → payload wrapper/typed assembly → direct-pose applicator。motion / auxiliary applicator dispatch 也新增子区标注，便于后续迁移时机械移动。
- **修改文件列表**：`train/models.py`；`tests/train/test_train_models_failfast.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：新增 `test_motion_joint_loss_motion_component_dispatch_order_regression` 与 `test_motion_joint_loss_aux_component_dispatch_order_regression`。`tests.train.test_train_models_failfast` 总数增至 `118`，联合 `tests.train.test_event_motion_model_refactor_phase_d` 总数为 `124`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`python3 - <<'PY' ... unittest.defaultTestLoader.loadTestsFromName(...) ...`；`(sed -n '5830,6555p' train/models.py; sed -n '2380,2535p' tests/train/test_train_models_failfast.py) | rg -n <removal-policy §6 regex>`；`rg -n "def _compute_direct_pose_group_base_payload|def _compute_direct_pose_group_norm_payload|def _compute_direct_pose_payload\\(|def _apply_motion_components|def _apply_aux_components" train/models.py`。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `124` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=6`），AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`，本轮 touched ranges 的 removal-policy 反模式 grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；Phase D 的 stage6 deterministic smoke / forward output snapshot 仍未在本 batch 复跑。
- **下一优先级热点**：inventory 视角下，D2 三个批次都保持 broad=`0`；下一轮若进入 D1，重点跟踪 `EventMotionModel.forward(...)` 的任何边界变化是否仍保持 broad=`0` 且不新增 removal-policy 反模式。

### Implementation Update — 2026-04-23 Phase D.D1 batch 1 EventMotionModel forward shell

- **本轮目标**：进入 Phase D.D1 第一批，只做 `EventMotionModel.forward(...)` 外壳收口；先建立 pre-change deterministic forward snapshot，再机械提取 input prep / runtime-control prep 与 final output assembly 薄壳，并补 stage6 deterministic smoke / snapshot 验证路径。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 新增 `_EventMotionForwardInputPrep`、`_prepare_forward_inputs(...)`、`_forward_input_shape_error(...)`、`_build_forward_base_result(...)`、`_write_forward_direct_pose_outputs(...)`。这些 helper 只承接现有 `EventMotionModel.forward(...)` 的输入归一化、runtime-control 读取、base result 构造与 direct-pose output key 写入，不触碰 contact-plan / event-clock / direct-pose 内部算法，不改 loss、输出 key、ckpt contract 或默认超参。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_forward_output_snapshot_deterministic_regression` 与 `test_forward_shell_dispatch_smoke_regression`；Phase D 测试总数增至 `8`，联合 `tests.train.test_train_models_failfast` 总数为 `126`。
- **stage6 deterministic smoke / snapshot 验证方式**：stage6 复用 roadmap §5.3 现有 executable path，输出目录 `debug_output/_tmp_train_models_phase_d_d1_stage6_smoke_20260423`；forward snapshot 复用 Phase D 测试 builder / fixture，固定 `torch.manual_seed(12345)` 造输入与 `torch.manual_seed(999)` 执行 eval forward，锁定 key set、shape、finite、sum/mean/L2 checksum。
- **运行过的命令**：pre-change inline snapshot Python；post-change inline snapshot Python；`python3 -m py_compile train/models.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_phase_d_d1_stage6_smoke_20260423 --run_name train_models_phase_d_d1_forward_shell_smoke_20260423 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；AST broad-handler inline Python；touched ranges removal-policy §6 grep。
- **验证结果**：pre/post forward snapshot checksum 一致；focused Phase D test 通过（`8` tests）；stage6 smoke 通过，`ok_steps=5 skipped=0`；最终联合验证通过后记录为 `tests.train.test_train_models_failfast=118`、`tests.train.test_event_motion_model_refactor_phase_d=8`、total=`126`，AST broad=`0` / exact=`0` / as_exc=`0`，touched ranges removal-policy grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；`EventMotionModel.forward(...)` 的 contact-plan / event-clock / direct-pose 主体仍未拆分，后续 D1 继续需要 snapshot 先行。
- **下一优先级热点**：继续 D1 第二批时，仍用 snapshot / dispatch smoke 保护输出 contract；优先收 contact-plan/event-clock 外壳边界，不进入 direct-pose stats contract、MotionJointLoss 数值逻辑或 Phase E 跨文件迁移。

### Implementation Update — 2026-04-24 Phase D.D1 batch 2 contact-plan / Event-Clock shell

- **本轮目标**：继续 Phase D.D1，只做 `EventMotionModel.forward(...)` 中 contact-plan / Event-Clock 外壳边界；保持 pre-change snapshot 先行，不改 direct-pose stats contract、MotionJointLoss 数值逻辑、输出 key、ckpt contract 或默认超参。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 新增 `_ContactClockForwardDefaults`、`_ContactPlanForwardFinal`、`_init_contact_clock_forward_defaults(...)`、`_finalize_contact_plan_outputs(...)`。这些 helper 只集中 Event-Clock/contact-plan 默认值初始化与 final stack/write-back，原有 contact-plan GRU loop、event-clock gate/corrector、direct-pose phase/side cue append 逻辑与异常上下文保持原样。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：更新 `test_forward_shell_dispatch_smoke_regression`，锁定 `_prepare_forward_inputs(...)` → `_init_contact_clock_forward_defaults(...)` → `_finalize_contact_plan_outputs(...)` → `_build_forward_base_result(...)` → `_write_forward_direct_pose_outputs(...)` 外壳调度；`test_forward_output_snapshot_deterministic_regression` 继续锁定 deterministic key/shape/finite/checksum。测试数量不变：Phase D tests=`8`，联合 total=`126`。
- **stage6 deterministic smoke / snapshot 验证方式**：stage6 复用 roadmap §5.3 executable path，输出目录 `debug_output/_tmp_train_models_phase_d_d1_contact_clock_smoke_20260424`；forward snapshot 复用 Phase D `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` / `torch.manual_seed(999)`，对比 key set、shape、finite、sum/mean/L2。
- **运行过的命令**：pre-change inline snapshot Python；post-change inline snapshot Python；`python3 -m py_compile train/models.py`；`python3 -m py_compile train/models.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_phase_d_d1_contact_clock_smoke_20260424 --run_name train_models_phase_d_d1_contact_clock_smoke_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；AST broad-handler inline Python；touched ranges removal-policy §6 grep。
- **验证结果**：pre/post forward snapshot checksum 一致；focused Phase D test 通过（`8` tests）；stage6 smoke 通过，`ok_steps=5 skipped=0`；最终联合验证通过后记录为 `tests.train.test_train_models_failfast=118`、`tests.train.test_event_motion_model_refactor_phase_d=8`、total=`126`，AST broad=`0` / exact=`0` / as_exc=`0`，touched ranges removal-policy grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；`EventMotionModel.forward(...)` 的 contact-plan observed init、Event-Clock on/off loop、direct-pose readout 与 leg residual 主体仍未拆分，后续 D1 继续需要 snapshot 先行。
- **下一优先级热点**：继续 D1 时，优先收 direct-pose readout / leg residual 外壳边界；若触及 direct-pose output/stats key 写入，必须复用现有 snapshot 与 stats contract regression，不进入 Phase E 跨文件迁移。

### Implementation Update — 2026-04-24 Phase D.D1 batch 3 direct-pose shell completion

- **本轮目标**：收完 Phase D.D1 外壳边界，只补 `EventMotionModel.forward(...)` 的 direct-pose entry / runtime-control shell 与 dispatch smoke；不改 direct-pose readout / leg residual 内部算法，不改 stats key、output key、ckpt contract、默认超参或 MotionJointLoss 数值逻辑。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无归类变化；`train/models.py` 新增 `_DirectPoseForwardRuntime`、`_should_run_direct_pose_forward(...)`、`_init_direct_pose_forward_runtime(...)`。这些 helper 只集中 direct-pose 执行条件、runtime override / ablation mode 与 direct-leg optional output default 初始化；direct-pose 主体仍保留原公式、原异常上下文与原 output key 写入 helper。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：更新 `test_forward_shell_dispatch_smoke_regression`，锁定 `_prepare_forward_inputs(...)` → `_init_contact_clock_forward_defaults(...)` → `_finalize_contact_plan_outputs(...)` → `_build_forward_base_result(...)` → `_should_run_direct_pose_forward(...)` → `_init_direct_pose_forward_runtime(...)` → `_write_forward_direct_pose_outputs(...)` 外壳调度；`test_forward_output_snapshot_deterministic_regression` 继续锁定 deterministic key/shape/finite/checksum。测试数量不变：Phase D tests=`8`，联合 total=`126`。
- **stage6 deterministic smoke / snapshot 验证方式**：stage6 复用 roadmap §5.3 executable path，输出目录 `debug_output/_tmp_train_models_phase_d_d1_complete_smoke_20260424`；forward snapshot 复用 Phase D `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` / `torch.manual_seed(999)`，对比 key set、shape、finite、sum/mean/L2。
- **运行过的命令**：pre-change inline snapshot Python；post-change inline snapshot Python；`python3 -m py_compile train/models.py`；`python3 -m py_compile train/models.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_phase_d_d1_complete_smoke_20260424 --run_name train_models_phase_d_d1_complete_smoke_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；AST broad-handler inline Python；touched ranges removal-policy §6 grep。
- **验证结果**：pre/post forward snapshot checksum 一致；focused Phase D test 通过（`8` tests）；stage6 smoke 通过，`ok_steps=5 skipped=0`；最终联合验证通过后记录为 `tests.train.test_train_models_failfast=118`、`tests.train.test_event_motion_model_refactor_phase_d=8`、total=`126`，AST broad=`0` / exact=`0` / as_exc=`0`，touched ranges removal-policy grep 零命中。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 只追加执行记录；D1 外壳已收口，但内部 direct-pose/leg residual 算法块仍在单文件 `forward(...)` 内保留，后续若拆出必须继续 snapshot 先行。
- **下一优先级热点**：Phase D.D1 可视为完成；inventory 视角下一步重点是 D 收尾时继续保持 broad=`0` 与 removal-policy grep 零命中，建议先做 Phase D.D3 / Phase D final snapshot + state_dict fingerprint，再考虑 Phase E。

### Implementation Update — 2026-04-24 Phase D.D3 final inventory / fingerprint closeout

- **本轮目标**：完成 Phase D 收尾，只做 `train/models.py` 结构 inventory、final deterministic forward snapshot / state_dict fingerprint 复核，以及 fail-fast inventory / roadmap 文档回填；不进入 Phase E，不改 `train/models.py` 核心算法、loss 数值语义、输出 key、checkpoint contract、默认超参。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` broad-handler 基线维持 `0`。本轮补充了 final structural inventory：`train/models.py` 当前 `LOC=6963`；`EventMotionModel.forward(...)` 位于 `train/models.py:3033`–`train/models.py:4820`，共 `1788` 行；`MotionJointLoss.forward(...)` 位于 `train/models.py:6947`–`train/models.py:6963`，共 `17` 行。当前主要 shell / cluster 为：forward input prep `train/models.py:2382`–`train/models.py:2494`、contact-plan / Event-Clock shell `train/models.py:2551`–`train/models.py:2655`、final output assembly `train/models.py:2498`–`train/models.py:2547`、direct-pose shell `train/models.py:2659`–`train/models.py:3032`、`MotionJointLoss` init cluster `train/models.py:4948`–`train/models.py:5240`、skeleton / weight-cache cluster `train/models.py:5241`–`train/models.py:5762`、payload cluster `train/models.py:6129`–`train/models.py:6595`、applicator cluster `train/models.py:5934`–`train/models.py:6128` 与 `train/models.py:6596`–`train/models.py:6814`、orchestration shell `train/models.py:6815`–`train/models.py:6963`。Phase D.D1 / D2 的已完成批次列表也已回填到 roadmap。
- **修改文件列表**：`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_state_dict_fingerprint_repeated_construction_regression`，并新增 `_tensor_sha256(...)` / `_state_dict_fingerprint(...)` helper；该回归复用 Phase D 现有 `_build_model(...)` builder，验证 repeated construction 下 `state_dict` 的 sorted key set、每个 tensor key 的 `shape` / `dtype` / `sha256(tensor.cpu().numpy().tobytes())` 与 aggregate fingerprint 不漂移。Phase D tests 总数增至 `9`，联合 `tests.train.test_train_models_failfast` 总数为 `127`。
- **final forward snapshot 验证方式**：继续复用 `tests.train.test_event_motion_model_refactor_phase_d.test_forward_output_snapshot_deterministic_regression`；固定 `torch.manual_seed(12345)` 生成输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **final state_dict fingerprint 验证方式**：运行 `tests.train.test_event_motion_model_refactor_phase_d.test_state_dict_fingerprint_repeated_construction_regression`；同一最小 split direct-pose builder 两次构造之间插入额外 RNG 消耗，确认 `state_key_count=93`、sample aggregate fingerprint `b6fc8c171a8855daca03735bba97dad40888c09edb2885822f7e7c6adfef2c80`，且 per-key metadata/sha256 全量一致。
- **stage6 deterministic smoke / snapshot 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_phase_d_final_smoke_20260424 --run_name train_models_phase_d_final_smoke_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`；forward snapshot 继续复用 Phase D test builder / fixture。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 - <<'PY' ... ast.parse(train/models.py) ...`；`(sed -n '1,260p' tests/train/test_event_motion_model_refactor_phase_d.py) | rg -n <removal-policy §6 regex>`；`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_phase_d_final_smoke_20260424 --run_name train_models_phase_d_final_smoke_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`；`python3 - <<'PY' ... _state_dict_fingerprint(model.state_dict()) ...`；`python3 - <<'PY' ... ast.parse(train/models.py) -> LOC/forward spans ...`。
- **验证结果**：`py_compile` 通过；联合 `unittest` 共 `127` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=9`）；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；touched code/test ranges removal-policy grep 零命中；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_phase_d_final_smoke_20260424` 并保存 `debug_output/_tmp_train_models_phase_d_final_smoke_20260424/ckpt_last_train_models_phase_d_final_smoke_20260424.pth`。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 继续只追加执行记录；当前 remaining risk 仍是单文件物理规模而非 broad-handler debt：`EventMotionModel.forward(...)` 仍为 `1788` 行，contact-plan / Event-Clock / direct-pose 主体算法块仍保留在文件内，后续若拆到 Phase E 需要继续 snapshot 先行。
- **当前 Phase D 是否可收尾**：可以；inventory 视角下 Phase D 的 broad=`0`、removal-policy grep 零命中、forward snapshot、state_dict fingerprint 与 stage6 deterministic smoke 已全部补齐。
- **下一步建议动作**：正式转入 Phase E 前，建议先冻结本轮 snapshot/fingerprint/line-inventory 作为迁移前基线，并为 planned cross-file move 列出 write-set / import surface / `_warn_once` 调整范围，确保下一阶段仍保持 purely mechanical move。

### 2026-04-24 — pre-Phase-E simplification batch 1 forward tail shell

- **本轮目标**：只做 `EventMotionModel.forward(...)` 尾段 `lambda_fusion` / `so3_delta_corrector` / `period_pred` 的 shell 收口，让后续单文件 simplification 更接近 mechanical move；不进入 Phase E，不改核心算法 / loss 数值语义 / 输出 key / checkpoint contract / 默认超参。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 新增 `_lambda_fusion_rollout_step_feature(...)`、`_write_forward_lambda_fusion_outputs(...)`、`_write_forward_so3_delta_outputs(...)`、`_write_forward_period_output(...)`。`forward(...)` 尾段从内联 tail block 收成 helper dispatch，`lambda_fusion rollout_step` contract 错误上下文保持原样，`omega_hat` / `period_pred` 写回 key 集保持不变。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **简化前后结构对照**：简化前为 `forward(...)` 尾段直接内联 `lambda_fusion` 输入拼接/rollout_step contract/head forward/writeback、`so3_delta_corrector` writeback、`period_pred` final writeback；简化后为 `_write_forward_lambda_fusion_outputs(...)`（内部再经 `_lambda_fusion_rollout_step_feature(...)`）→ `_write_forward_so3_delta_outputs(...)` → `_write_forward_period_output(...)` 的连续尾段壳层。当前 `forward(...)` 位于 `train/models.py:3194`，尾段 helper 位于 `train/models.py:2549`、`train/models.py:2619`、`train/models.py:2678`、`train/models.py:2702`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`。
- **新增 / 更新测试**：更新 `tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_shell_dispatch_smoke_regression`，将 tail dispatch 锁定为 `_write_forward_lambda_fusion_outputs(...)` → `_write_forward_so3_delta_outputs(...)` → `_write_forward_period_output(...)`；`test_forward_output_snapshot_deterministic_regression` 与 `test_state_dict_fingerprint_repeated_construction_regression` 直接复用。测试总数保持：`tests.train.test_train_models_failfast=118`、`tests.train.test_event_motion_model_refactor_phase_d=9`、total=`127`。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`；复用 `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` 造输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；继续验证 repeated construction 下 sorted key set、per-key `shape` / `dtype` / `sha256` 与 aggregate fingerprint 不漂移。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch1_20260424 --run_name train_models_pre_phase_e_simplify_batch1_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched diff removal-policy grep。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `127` 个用例通过；snapshot 单测通过；state_dict fingerprint 单测通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch1_20260424` 并保存 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch1_20260424/ckpt_last_train_models_pre_phase_e_simplify_batch1_20260424.pth`。
- **阻塞项 / 风险**：本轮只压平尾段 shell，没有进入 direct-pose leg gate/scale duplicated runtime block；`forward(...)` 仍保留 direct-pose / contact-plan / Event-Clock 主体算法大块。联合单测中出现的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 warning-only debt。
- **下一步建议动作**：继续 pre-Phase-E simplification batch 2，优先收 direct-pose leg gate / scale duplicated runtime block 的外壳或 dispatch；暂不进入 Phase E 跨文件迁移。

### 2026-04-24 — pre-Phase-E simplification batch 2 direct-pose leg gate/scale shell

- **本轮目标**：继续单文件 simplification，只收 `EventMotionModel.forward(...)` 中 direct-pose leg learned gate / scale gate duplicated runtime block；不进入 Phase E，不改 direct-pose 数值语义、输出 key、checkpoint contract、默认超参。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 新增 `_DirectPoseLegGateOutputs` 与 `_apply_direct_pose_leg_gate_outputs(...)`，把 non-side 与 side-routed 两条路径中重复的 learned/scale gate 逻辑统一到同一薄 helper。`forward(...)` 仍在各自分支内负责构造 `omega_leg` 与 side scatter index，但 learned gate 的 `sigmoid/power`、scale gate 的 `exp/clamp/log`、以及 `direct_leg_gate*` / `direct_leg_scale*` writeback 现都通过共享 helper 完成。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **简化前后结构对照**：简化前为 `forward(...)` 内两套 duplicated gate/scale runtime：一套给 side-routed `direct_pose_leg_gate_head_shared`，一套给 non-side `direct_pose_leg_gate_head`；两套都各自重复 head forward、power/clamp、scale/log writeback。简化后两条路径都改为只准备各自 `head_inputs` / `side_positions`，再统一调用 `_apply_direct_pose_leg_gate_outputs(...)`；side-routed 与 non-side 的错误标签与 missing-head 上下文保持分支特异。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_forward_leg_gate_helper_dispatch_regression` 与 `test_forward_leg_scale_helper_dispatch_regression`，分别覆盖 learned / scale 两种 gate mode，并在 `side_routing=False/True` 下锁定 `_apply_direct_pose_leg_gate_outputs(...)` 的 dispatch；Phase D tests 总数增至 `11`，联合 `tests.train.test_train_models_failfast` 总数为 `129`。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`；继续复用 `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` 造输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；继续验证 repeated construction 下 sorted key set、per-key `shape` / `dtype` / `sha256` 与 aggregate fingerprint 不漂移。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch2_20260424 --run_name train_models_pre_phase_e_simplify_batch2_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched ranges removal-policy grep。
- **验证结果**：通过；`py_compile` 通过，联合 `unittest` 共 `129` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=11`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch2_20260424` 并保存 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch2_20260424/ckpt_last_train_models_pre_phase_e_simplify_batch2_20260424.pth`。
- **阻塞项 / 风险**：本轮只统一了 gate/scale apply 壳层；side-routed 与 non-side 的 upstream feature assembly、per-side cue/embedding、omega scatter 仍保留在 `forward(...)` 内。联合单测里出现的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 warning-only debt。
- **下一步建议动作**：继续 pre-Phase-E simplification batch 3，优先收 side-routed / non-side leg feature assembly 与 `omega_leg` pre-gate shell；仍暂不进入 Phase E 跨文件迁移。

### Implementation Update — 2026-04-24 pre-Phase-E simplification batch 3 leg assembly / cue-embed / omega pre-gate shell

- **本轮目标**：继续单文件 simplification，只收 direct-pose leg residual 路径里的 side-routed / non-side feature assembly、side-routed cue / embedding shell、以及 `omega_leg` pre-gate shell；不改 direct-pose 数值语义、输出 key、checkpoint contract、默认超参，也不引入 warning-only debt。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 新增 `_DirectPoseSideLegAssembly`、`_prepare_direct_pose_leg_head_input(...)`、`_prepare_direct_pose_side_cues(...)`、`_prepare_direct_pose_side_embeddings(...)`、`_prepare_direct_pose_leg_omega(...)`、`_assemble_direct_pose_side_leg_features(...)`。这些 helper 只收拢 shape / contract / error context 与 duplicated pre-gate prep，不改变 direct-pose leg residual 主体公式；既有 `direct_pose side-routed phase_z contract failed`、`direct_pose side-routed side embedding forward failed`、`direct_pose leg residual forward failed` 等错误边界保持可见。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_forward_leg_input_helper_dispatch_regression`、`test_forward_side_leg_cue_embedding_shell_dispatch_regression`、`test_forward_leg_omega_pre_gate_helper_dispatch_regression`；与既有 gate/scale dispatch 回归配合后，Phase D tests 总数增至 `14`，联合 `tests.train.test_train_models_failfast` 总数增至 `132`。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`；继续复用 `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` 造输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；继续验证 repeated construction 下 sorted key set、per-key `shape` / `dtype` / `sha256` 与 aggregate fingerprint 不漂移。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch3_20260424 --run_name train_models_pre_phase_e_simplify_batch3_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched code/test ranges removal-policy grep。
- **验证结果**：通过；联合 `unittest` 共 `132` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=14`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch3_20260424`。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 继续只追加执行记录；remaining risk 仍是 `forward(...)` 的物理体量，而不是 broad-handler / warning-only / compat debt。联合单测中的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 `warnings.warn(...)` 直写路径。
- **下一优先级热点**：继续 pre-Phase-E simplification batch 4，优先收 side-routed / non-side leg head forward 之后的 residual orchestration 壳层；仍暂不进入 Phase E。

### Implementation Update — 2026-04-24 pre-Phase-E simplification batch 4 side omega / non-side delta dispatch shell

- **本轮目标**：继续单文件 simplification，只收 side-routed sign-gate / rank1 / side-omega resolver，以及 non-side cross-leg ablation / head fallback / `rot6d_add` residual writeback 的 dispatch shell；不改 direct-pose 数值语义、输出 key、checkpoint contract、默认超参，也不引入 warning-only debt。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 新增 `_DirectPoseSideLegOmegaOutputs`、`_resolve_direct_pose_side_leg_omegas(...)`、`_resolve_direct_pose_non_side_leg_delta(...)`、`_apply_direct_pose_rot6d_leg_delta(...)`。这些 helper 只收拢 branch tail dispatch 与 shape/error context，不改变 direct-pose leg residual 主体公式；既有 `direct_pose side-routed leg sign gate forward failed`、`direct_pose leg residual forward failed` 等错误边界保持可见。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_forward_side_leg_omega_resolver_dispatch_regression`、`test_forward_non_side_leg_delta_dispatch_regression`、`test_forward_non_side_rot6d_residual_writeback_dispatch_regression`；Phase D tests 总数增至 `17`，联合 `tests.train.test_train_models_failfast` 总数增至 `135`。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`；继续复用 `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` 造输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；继续验证 repeated construction 下 sorted key set、per-key `shape` / `dtype` / `sha256` 与 aggregate fingerprint 不漂移。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch4_20260424 --run_name train_models_pre_phase_e_simplify_batch4_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched code/test ranges removal-policy grep。
- **验证结果**：通过；联合 `unittest` 共 `135` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=17`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch4_20260424`。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 继续只追加执行记录；remaining risk 仍是 `forward(...)` 的物理体量，而不是 broad-handler / warning-only / compat debt。联合单测中的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 `warnings.warn(...)` 直写路径。
- **下一优先级热点**：继续 pre-Phase-E simplification batch 5，优先收 final direct-pose leg residual writeback / result assignment 壳层；仍暂不进入 Phase E。

### Implementation Update — 2026-04-24 pre-Phase-E simplification batch 5 leg residual final writeback / result-assignment shell

- **本轮目标**：继续单文件 simplification，只收 direct-pose leg residual 的 final writeback / result-assignment dispatch shell；不改 direct-pose 数值语义、输出 key、checkpoint contract、默认超参，也不引入 warning-only debt。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 新增 `_DirectPoseLegWritebackOutputs` 与 `_dispatch_direct_pose_leg_residual_writeback(...)`，并让 `_write_forward_direct_pose_outputs(...)` 从散列参数列表改为消费一个明确的 writeback contract。side-routed / non-side 两条路径现在都会在各自 resolver 之后进入统一的 final dispatch 壳，再写回 `out_direct` 与 `direct_leg_*` 输出。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_forward_leg_residual_final_writeback_shell_regression`；Phase D tests 总数增至 `18`，联合 `tests.train.test_train_models_failfast` 总数增至 `136`。该回归最小锁定 side-routed `scale` / `sign-gate` / `rank1`、non-side `learned` / `rot6d_add` 在 resolver 之后都会进入 `_dispatch_direct_pose_leg_residual_writeback(...)`，并保持 output key contract。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch5_20260424 --run_name train_models_pre_phase_e_simplify_batch5_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched code/test ranges removal-policy grep。
- **验证结果**：通过；联合 `unittest` 共 `136` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=18`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch5_20260424`。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 继续只追加执行记录；remaining risk 仍是 `forward(...)` 的物理体量，而不是 broad-handler / warning-only / compat debt。联合单测中的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 `warnings.warn(...)` 直写路径。
- **下一优先级热点**：继续 pre-Phase-E simplification batch 6，优先看 direct-pose readout / writer 之间是否还能再压出一层更线性的 orchestration 壳；仍暂不进入 Phase E。

### Implementation Update — 2026-04-24 pre-Phase-E simplification batch 6 corrective shell + thin-helper retirement

- **本轮目标**：对 batch 5 做一次 corrective simplification，撤回会制造 distributed cognition 的薄 helper / kwargs-heavy dispatch，只保留少而硬的 contract helper 与 branch-sized shell；不改任何 fail-fast boundary、direct-pose 数值语义、输出 key、checkpoint contract、默认超参，也不引入 warning-only debt。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 退休 `_prepare_direct_pose_side_cues(...)`、`_prepare_direct_pose_side_embeddings(...)`、`_prepare_direct_pose_leg_head_input(...)`、`_apply_direct_pose_rot6d_leg_delta(...)`、`_dispatch_direct_pose_leg_residual_writeback(...)`、`_DirectPoseLegWritebackOutputs`，并新增 `_forward_side_routed_leg_residual(...)`、`_forward_non_side_leg_residual(...)` 作为 branch-sized shell。保留的 `_assemble_direct_pose_side_leg_features(...)`、`_prepare_direct_pose_leg_omega(...)`、`_apply_direct_pose_leg_gate_outputs(...)`、`_resolve_direct_pose_side_leg_omegas(...)`、`_resolve_direct_pose_non_side_leg_delta(...)` 继续承载真实 shape / contract / error context；既有 `direct_pose side-routed leg residual forward failed` 与 `direct_pose leg residual forward failed` 错误边界保持可见。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **简化前后结构对照**：batch 5 把 final writeback / result-assignment 收成 dataclass + dispatch helper，但主 branch control flow 仍旧迷宫化。batch 6 改为让 `forward(...)` 外层只做 branch dispatch，真正的 side-routed / non-side residual orchestration 下沉到两个 shell；其中 non-side shell 通过 guard clause 提前返回，直接压平旧的 `if idx -> if K -> if rot_dim -> if idx_in_range -> if leg_mode` 金字塔。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 删除对已退休薄 helper 的 dispatch regression，改为新增 `test_forward_side_routed_leg_residual_shell_dispatch_regression` 与 `test_forward_non_side_leg_residual_shell_dispatch_regression`。Phase D tests 当前为 `16`，联合 `tests.train.test_train_models_failfast` 当前为 `134`；这些回归最小锁定 side-routed / non-side shell dispatch 与 learned / scale / rank1 / sign-gate / rot6d_add / so3 下的 output key contract。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch6_20260424 --run_name train_models_pre_phase_e_simplify_batch6_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；snapshot 单测命令；state_dict fingerprint 单测命令；AST broad-handler inline Python；touched code/test ranges removal-policy grep；stage6 deterministic smoke 命令。
- **验证结果**：通过；联合 `unittest` 共 `134` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=16`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；touched ranges removal-policy grep 对 nested fallback / `warnings.warn(...)` / `state_dict[...] = state_dict.pop(...)` / `# compat` / `# legacy` 均为 `0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch6_20260424`。
- **阻塞项 / 风险**：本轮没有新的 fail-fast boundary 变化，因此 inventory 继续只追加执行记录；remaining risk 不再是 broad-handler / warning-only / compat debt，而是 direct-pose 主 readout/orchestration 的剩余宿主体量。联合单测中的 `[Loss][UnifiedWeights]` `UserWarning` 仍来自既有 `_warn_once` 路径，本轮未新增 `warnings.warn(...)` 直写路径。
- **为什么此时仍适合继续单文件 simplification**：纠正 batch 5 之后，`forward(...)` 的 residual 入口已重新回到两个局部可读的 branch shell；这意味着如果再做一小步单文件整理，成本形态仍是 localized duplication，而不是跨多 helper 的上下文搬运。换句话说，当前仍处于“可以机械验证的小步整理”区间。
- **下一优先级热点**：先暂停评估是否直接进入 Phase E；若仍坚持单文件 simplification，最推荐只看 direct-pose 主 readout/orchestration 的单个宿主壳，不再新增薄 helper，不建议继续扩展 batch 5 风格的 dispatch 层。

### Implementation Update — 2026-04-24 pre-Phase-E simplification batch 7 event-clock loop shell

- **本轮目标**：只处理 `EventMotionModel.forward(...)` 中 event-clock on 分支的 contact-plan GRU loop body，把 per-step phase/cue append、event-clock gate/corrector、optional time-bias 壳压平成更线性的宿主阅读路径；不碰 `__init__`、`_canonicalize_contacts_meas_inputs(...)`、direct-pose leg residual 路径、Phase E 跨文件迁移，也不改任何数值语义 / 输出 key / checkpoint contract。
- **实际完成项**：fail-fast inventory 本轮无新增条目、无删除、无重分类；`train/models.py` 在 `forward(...)` 内新增 2 个 nested def：共享的 `_append_contact_plan_direct_step_inputs(...)` 收拢 per-step `phase_in_direct_seq.append(...)` / `leg_side_cue_seq.append(...)` 的 shape 校验与错误包装，event-clock on 专用 `_step_contact_plan_event_clock(...)` 承接 `plan_z raw/logits/err -> gate -> corrector -> optional time bias -> debug/logit/prob append -> lambda/dyn/delta append` 的整段 loop shell。event-clock off 分支只复用前者，因此外层 loop 更短，但没有新增薄 helper 网络。
- **修改文件列表**：`train/models.py`；`tests/train/test_event_motion_model_refactor_phase_d.py`；`docs/changes/2026-04-21_train_models_fail_fast_inventory.md`；`docs/changes/2026-04-21_train_models_single_file_refactor_roadmap.md`；`docs/delete/2026-04-24_train_models_pre_phase_e_cleanup_audit.md`。
- **当前统计**：broad `Exception` handler 维持 `0 -> 0`；精确 `except Exception:` 维持 `0 -> 0`；`except Exception as exc` 维持 `0 -> 0`；inventory 条目集合无新增、无删除、无重分类。
- **简化前后结构对照**：batch 6 后 event-clock on loop 仍内联承载 3 类噪音：phase/cue per-step contract、gate/corrector read path、time-bias try/except 壳。batch 7 后 `forward(...)` 的 event-clock loop 宿主接近“step shell -> finalize stack”，shell 内顺序固定为 `phase/cue append -> plan_z_raw/logits/err -> gate -> corrector -> time bias -> debug/logits/probs -> lambda stats append`；保留的 duplication 只剩 event-clock off 路径里局部可 scroll 的 base contact-plan readout。
- **新增 / 更新测试**：`tests.train.test_event_motion_model_refactor_phase_d` 新增 `test_event_clock_loop_shell_phase_cue_time_bias_regression`，最小覆盖 event-clock on 路径确实同时经过 phase append / cue append / optional time-bias，并保持 `event_clock_*` / `contacts_plan*` / `out_direct` key contract。Phase D tests 当前为 `17`，联合 `tests.train.test_train_models_failfast` 当前为 `135`。
- **forward snapshot 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression`；继续复用 `_make_forward_snapshot_output()`，固定 `torch.manual_seed(12345)` 造输入、`torch.manual_seed(999)` 执行 eval forward，锁定 output key set、关键 tensor shape、finite、sum/mean/L2 checksum。
- **state_dict fingerprint 验证方式**：`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；继续验证 repeated construction 下 sorted key set、per-key `shape` / `dtype` / `sha256` 与 aggregate fingerprint 不漂移。
- **stage6 deterministic smoke 验证方式**：`PYTHONPATH=. debug_output/_tmp_phasecd_min_ablation_20260330/cpu_nomps_exec.py -m train.posttrain --config debug_output/_target_stage6_same_donor_det_20260421/current_code/config_stage6_offset0_e8x60.json --out_dir debug_output/_tmp_train_models_pre_phase_e_simplify_batch7_20260424 --run_name train_models_pre_phase_e_simplify_batch7_20260424 --epochs 1 --steps_per_epoch 5 --save_step_ckpts 0,1,5 --rollout_random_offset false --seed 0`。
- **运行过的命令**：`python3 -m py_compile train/models.py tests/train/test_train_models_failfast.py tests/train/test_event_motion_model_refactor_phase_d.py`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_event_clock_loop_shell_phase_cue_time_bias_regression`；`python3 -m unittest tests.train.test_train_models_failfast tests.train.test_event_motion_model_refactor_phase_d`；`python3 -m unittest tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_forward_output_snapshot_deterministic_regression tests.train.test_event_motion_model_refactor_phase_d.EventMotionModelRefactorPhaseDTest.test_state_dict_fingerprint_repeated_construction_regression`；AST broad-handler inline Python；stage6 deterministic smoke 命令；touched ranges removal-policy grep。
- **验证结果**：通过；`py_compile` 通过；focused event-clock loop-shell regression 通过；联合 `unittest` 共 `135` 个用例通过（`tests.train.test_train_models_failfast=118`，`tests.train.test_event_motion_model_refactor_phase_d=17`）；snapshot / state_dict fingerprint 单测均通过；AST 计数得到 broad=`0` / exact=`0` / as_exc=`0`；stage6 smoke 通过，`ok_steps=5 skipped=0`，输出目录 `debug_output/_tmp_train_models_pre_phase_e_simplify_batch7_20260424`；touched ranges removal-policy grep 对 nested fallback / `warnings.warn(...)` / `state_dict[...] = state_dict.pop(...)` / `# compat` / `# legacy` 均为 `0`。
- **阻塞项 / 风险**：本轮没有任何 fail-fast boundary 变化；remaining risk 也不在 broad-handler / compat debt，而在于 batch 7 之后“还要不要继续单文件 simplification”。当前 event-clock off loop 仍保留一点 branch-local duplication，但再继续抽壳，很容易重新滑向 batch 5 式的 thin-helper 分散阅读成本。
- **为什么此时仍适合继续单文件 simplification**：batch 7 之所以成立，是因为 event-clock loop 仍有一个明确的 host-sized shell，可用 2 个 nested def 在 closure 局部内完成压平，不需要 module method、kwargs 爆炸或 dataclass return。这正好符合“localized duplication > distributed cognition”的最后一类低风险整理。
- **下一优先级热点**：默认建议把 batch 7 视为 pre-Phase-E simplification 的收束点，先转 Phase E readiness 评估；如果强制还要再做一批，最多只看 contact-plan non-event-clock / finalize handoff 的单个宿主壳，且完成后应立即停止，不建议继续扩展 batch 8+。
